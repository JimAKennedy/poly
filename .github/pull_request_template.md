## Summary

Brief description of what this PR does and why.

## Changes

-

## Testing

- [ ] `cmake --build build && ctest --test-dir build` passes
- [ ] clang-format applied to all modified C++ files
- [ ] `scripts/check-realtime-safety.sh` passes (if engine/plugin files changed)
- [ ] Tested in Cubase (if UI or MIDI output changed)

## Notes

Any additional context for reviewers.
