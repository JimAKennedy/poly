{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "copy-webui": "mkdir -p public/webui && cp ../webui/index.html ../webui/ui.js ../webui/ui.css ../webui/host-iface.js ../webui/groove-math.js ../webui/smf-writer.js ../webui/dump-mode.js ../webui/mock-host.js ../webui/wasm-host.js ../webui/plugin-host.js public/webui/ && (cp ../webui/poly_engine.js ../webui/poly_engine.wasm public/webui/ 2>/dev/null || true)",
    "generate-presets": "node scripts/generate-presets-json.mjs",
    "generate-counts": "node scripts/generate-counts-json.mjs",
    "generate-params": "node scripts/generate-params-json.mjs",
    "dev": "npm run generate-presets && npm run generate-counts && npm run generate-params && astro dev",
    "start": "npm run generate-presets && npm run generate-counts && npm run generate-params && astro dev",
    "build": "npm run generate-presets && npm run generate-counts && npm run generate-params && npm run copy-webui && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "node --test tests/**/*.test.mjs",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.4",
    "@fontsource-variable/inter": "^5.3.0",
    "@fontsource-variable/jetbrains-mono": "^5.3.0",
    "@fontsource-variable/source-serif-4": "^5.3.0",
    "astro": "^7.1.3",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "node-web-audio-api": "^2.1.0"
  }
}
