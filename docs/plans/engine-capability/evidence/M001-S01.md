---
class: gated
---

# M001/S01 — evidence

## M001/S01 — task 1: `theory-balkan` Humanize column and Rule 7's unit

- `format` → exit 0
- `site-unit` → exit 0, 272 tests, 0 failures (up from 271 — the new case)
- `doc-conformance` → exit 0, 251 tests, 0 failures
- `doc-discipline` → exit 0

The gate bites. Registering `balkan-humanize-bound` before the column existed
failed with all four lanes reporting `no Humanize cell`, which also proved the
`CHECKLIST.push` landed above the registration loop rather than below it. After
the column was added, raising `Melodic accents` from `4ms` to `12ms` failed the
named case with `Melodic accents at 12ms`; reverted, and the file holds no
`12ms` cell.

The predicate compares with a negated `<=` so an absent or unparseable cell
reads as `NaN` and fails, rather than passing silently.

- 2026-09-12
