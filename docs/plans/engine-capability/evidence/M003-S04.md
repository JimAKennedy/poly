## M003/S04 — tasks 1 and 2: the guide catches up

- `format` → exit 0
- `site-unit` → exit 0, 287 tests, 0 failures
- `doc-conformance` → exit 0
- `doc-discipline` → exit 0
- `ledger` → exit 0, conforms with the slice `done`

Committed together: adding the `Timing` column without flipping Rule 6's verdict
leaves the tree red by design. M007's guard — "a not-checkable verdict naming an
absent column is telling the truth" — fired the moment the column appeared:

> `theory-brazilian.mdx rule 6 is excused because there is no Timing column, but
> the table has one`

That is a check from a different programme catching this one mid-edit, and it is
the reason the two tasks are one commit.

Rule 6's verdict goes from `absentColumn: 'Timing'` to
`checkable: case: 'bra-lssl-profile'`. Three arms proved, each restored by an
explicit edit rather than `git checkout`:

| Mutation | Result |
|---|---|
| profile flattened to `1.0 1.0 1.0 1.0` | **fails** — `EC11`'s stated verification |
| profile made a swing-shaped alternation `1.06 0.94 1.06 0.94` | **fails** — "an even long-short alternation is swing, not the Rule 6 profile" |
| swing left on a profiled lane | **fails** — "carries both a profile and swing" |

The second arm is the one worth having. Rule 6's whole claim is that swing
approximates the feel *poorly* because it displaces only alternate notes, so a
predicate that a two-value alternation could satisfy would assert nothing the
rule cares about.

The "until subdivision profiles ship" clause is gone from Rule 6, and the
patch's construction step 6 no longer describes swing as the approximation: the
1/16 lanes carry the profile at Swing 0, while the 1/8 surdo and the repinique
keep their swing, because Rule 6 is a statement about the sixteenths within a
beat.

- 2026-09-15
