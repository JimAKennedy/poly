## M003/S01 — task 1: the profile fields and `computeAdditiveCells`

- `format` → exit 0 (clang-format reflowed the new header block on the first
  pass; clean on re-run)
- `unit` → exit 0, 608 tests, 0 failures
- `engine-isolation` → exit 0, 485 tests, 0 failures

Written test-first. The five cases failed to compile — `no member named
'profileCount' in 'poly::LaneConfig'` — which is the right failure for a field
that does not exist yet.

What the cases pin, beyond "it works":

| Case | What it stops |
|---|---|
| stated proportions | a profile that places steps evenly after all |
| scale invariance | a profile summing to 4.2 lengthening its lane 5% and drifting the ensemble apart |
| integer aksak untouched | the `{2,2,3}` davul moving |
| precedence over `cellSizes` | two non-isochronies combining silently |
| non-positive sum rejected | a division by zero reaching the timing path |

**Two findings, both recorded in `M003-decisions.md` rather than fixed quietly.**

`GrooveStateCopyBenchmark.ReportsFactSizes` failed: `GrooveState` grows
13776 → 15888 bytes, +2112, the largest single growth the struct has taken. The
guard exists to force a conscious decision, so it was answered with a
measurement rather than a new constant — the three-copy pipeline runs at
**0.45 µs/block, 0.02% of a 128-sample block period**, and
`ThreeCopyPipelineFitsBlockBudget` passes.

The first test run failed at a `1e-9` tolerance because `1.1f / 0.9f` differs
from the double ratio by about 6e-8. The engine was right and the test was
wrong: the profile is stored as `float` by design, so a tolerance tighter than
float precision tests the storage type rather than the arithmetic.

- 2026-09-15

## M003/S01 — task 2: wired through `prepareLaneContext`

- `format` → exit 0
- `unit` → exit 0, 612 tests, 0 failures
- `engine-isolation` → exit 0, 489 tests, 0 failures
- `rt-safety` → exit 0

**All three new cases passed on their first run, which proves nothing**, so each
was mutated against: a probe discarding the profile in `prepareLaneContext`.
The first run of that probe is the finding:

| Case | Under the probe |
|---|---|
| differs from the even grid and the swung grid | **failed** |
| holds its ratios across tempi | *passed* — vacuous |
| does not lengthen the cycle | *passed* — vacuous |

An even grid also holds its ratios across tempi, and also does not lengthen the
cycle, so two of the three cases could not fail. Both were strengthened to pin
that the intervals are uneven before asserting anything about them, and the
probe re-run: **all three fail with the profile ignored, all three pass with it
honoured.** This is the M007 finding recurring in new code — a predicate that
cannot fail reads exactly like one that passes — and it took a mutation round to
see, not a reading of the test.

**A bound was wrong, and the test written for it does not catch it.** On the
profile path `cfg.cellSizes[c]` is unset, so `maxStepDur` stayed at the base
step while a profiled step can be 1.1× longer, under-covering the lookahead by
about 0.0008 PPQ. `SplitRenderingMatchesWholeRendering` — nineteen split points
across the cycle — **passes both before and after the fix**, because the
additive path enumerates whole cycles with a one-cycle margin that swamps the
shortfall. The bound is now derived from `cumPpq`, which is correct on both
paths. The fix is correctness of the bound, not a demonstrated bug, and the test
is a regression guard rather than a proof; saying otherwise would be claiming a
mutation proof that was not obtained.

- 2026-09-15

## M003/S01 — task 3: state version 19 → 20

- `format` → exit 0
- `unit` → exit 0, 614 tests, 0 failures
- `engine-isolation` → exit 0, 491 tests, 0 failures

Follows `kKotekanModeStateVersion` exactly — the most recent precedent, in the
same file. `profileCount` is written first and only that many floats follow, so
a lane with no profile costs four bytes rather than 260. The reader rejects a
count outside `[0, kMaxSteps]` rather than trusting it, because a forged count
would otherwise walk it off the end of the array.

Both cases were written after the implementation, so each was mutated against,
and the two probes are complementary:

| Probe | Result |
|---|---|
| write side never emits the profile | `SubdivisionProfileRoundTrips` **fails**; the pre-v20 case passes |
| read guard lowered to `>= 19` | `PreV20StateDefaultsToTheEvenGrid` **fails**; the round-trip passes |

Each probe kills exactly one case, which is the evidence they test different
things rather than the same thing twice.

**A stale build reported a false red.** After restoring the second probe, the
suite still showed `PreV20StateDefaultsToTheEvenGrid` failing against sources
that were provably correct — `git diff` showed the guard back in place. The
header change had not been recompiled; `touch`ing the headers and rebuilding
turned it green. The hazard runs both ways, and the dangerous direction is the
other one: a probe that *should* have failed reading as passed. Every mutation
result in this slice was taken from a build that had just compiled the edit.

- 2026-09-15

## M003/S01 — task 4: the samba profile ships, and reaches `presets.json`

- `format` → exit 0
- `unit` → exit 0, 614 tests, 0 failures
- `engine-isolation` → exit 0, 491 tests, 0 failures
- `rt-safety` → exit 0

`presets.json` reads `schemaVersion: 6`, 45 presets, and the Samba Batucada
lanes 1, 2 and 4 — tamborim, agogo, caixa — each carry
`profileCount: 16` with `[1.08, 0.94, 0.94, 1.04]` repeating across four beats.

**The three profiled lanes previously carried `swingAmount` 0.25, 0.20 and
none** — literally the "light swing (0.15–0.25) plus small per-lane offsets"
that Rule 6 admits is an approximation. The profile replaces it on those lanes
rather than stacking on top of it. The surdo and repinique keep their swing:
they run at quarter and eighth notes, and Rule 6 is a statement about the four
sixteenths within a beat.

Two things checked rather than assumed:

- **The generator's schema gate is not vacuous.** The first regeneration
  attempt raised the version in the emitter's doc comment but not in the string
  it emits, and the generator refused: `unexpected schemaVersion 5 —
  regenerate/update consumers`. A gate that catches its author on the first try
  is a gate.
- **`generate-presets-json.mjs` already rebuilds the emitter every run.** M005's
  PIPE01 removed the `existsSync` early return here, and the comment above it
  records the M001/S02 incident that caused it — 43 presets written after the
  engine had 44. There was no third instance to fix; the plan asked for the
  check and the check found it already done.
- `site/public/webui/presets.json` is also written, and is gitignored build
  output rather than a tracked artifact — verified with `git check-ignore`, not
  inferred from its absence in `git status`.

- 2026-09-15

## M003/S01 — task 5: close-out

Whole validation set, re-run on the final tree rather than carried forward from
the tasks that landed it:

- `format` → exit 0
- `unit` → exit 0, 614 tests, 0 failures
- `engine-isolation` → exit 0, 491 tests, 0 failures
- `rt-safety` → exit 0
- `ledger` → exit 0, conforms with the slice `done`

The slice's four definition-of-done claims, each against what actually ran:

| Claim | Evidence |
|---|---|
| a lane plays a non-isochronous profile | 9 engine cases, 3 of them mutation-proved against a probe that discards the profile |
| expressible in a preset, reaches `presets.json` | `schemaVersion: 6`, Samba Batucada lanes 1/2/4 at `profileCount: 16` |
| `renderRange()` gains no allocation, lock or blocking call | `rt-safety` exit 0; the profile is a fixed-size member array read in place |
| the engine builds and passes with no VST3 SDK | `engine-isolation`, 491 tests |

**What this slice found, beyond what it built.** Two of its own render-level
tests were vacuous on first writing and only a mutation round showed it; a
lookahead bound read a field meaningless on the path it guards; and a stale
build reported a red against correct sources. None of the three came from
reading the code. All three are recorded in `M003-decisions.md`.

- 2026-09-15
