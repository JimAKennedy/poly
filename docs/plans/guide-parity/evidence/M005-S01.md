## M005/S01 — task 1: the `sanitizers` token runs here

First execution of the token since it was added at assessment. All five variants
configured, built and ran on Darwin arm64 / Apple clang 21.

| # | Variant | Tests | Result | Test time |
|---|---|---|---|---|
| 1 | ASan, engine-only | 568 | pass | 35.1 s |
| 2 | UBSan, engine-only | 568 | pass | 9.3 s |
| 3 | TSan, engine-only | 568 | pass | 173.1 s |
| 4 | ASan, plugin | 55 + 59 Bridge | pass | 5.6 s + 4.8 s |
| 5 | TSan, plugin | 55 | pass | 26.4 s |

- `format` → exit 0
- `sanitizers` → exit 0, all five variants clean
- 2026-09-19

**Wall clock, configure and build included: roughly six minutes**, dominated by
TSan engine-only at just under three. That is affordable to run before a push,
which matters — a local sanitizer command nobody can afford is a token in name
only.

**The TSan-only twin does compile in here.** The plan required confirming rather
than assuming this, because `POLY_TSAN_ACTIVE` keys off
`__has_feature(thread_sanitizer)`. `ctest -N` lists both
`HostTests.HandshakeStress_NoTearNoLoss` (#30) and
`HostTests.HandshakeStress_TSanClean` (#31), so the guard fired and the filed
test was genuinely exercised — not skipped into a false pass.

**No finding other than the handshake race appeared**, and the handshake race did
not appear either on this first run. Task 2 takes that up.

## M005/S01 — task 3: what stopped TSan reporting on 2026-08-17

**Answered. It was a real fix, not a narrowing — and that makes the remaining
question sharper, not softer.**

The cheap eliminations first, by reading rather than building:

| Candidate | Changed in the window? |
|---|---|
| `.github/workflows/sanitizers.yml` — filter or target | **no** |
| `tests/host/host_tests.cpp` | yes, +158 lines |
| `plugin/source/` handshake implementation | yes, 21 files |

`plugin/source/processor.h` carries the answer in its own comment. Commit
`076f545` replaced the host→RT exchange with a lockless **triple buffer**, and
names the defect it was fixing:

> the prior design used two slots and an alternating writer index … after the
> reader's `exchange(-1)` the writer had NO way to know the reader was still
> copying `slots[cur]`, so the writer's second alternation could write
> `slots[cur]` concurrently with the reader's detach copy (**TSan data race on
> the `slots[]` detach copy, all five hot slots** — pinned in M031 S01 T01). Two
> slots are fundamentally insufficient …

That lands on 2026-08-16, which is exactly when the 22-night streak stopped. The
TSan data race was diagnosed and fixed by other work, before this milestone
existed. Nothing was suppressed and no window was narrowed.

**What that leaves is the harder finding.** The handshake has been stable since:

| File | Last changed |
|---|---|
| `plugin/source/processor.h` | 2026-08-16 |
| `tests/host/host_tests.cpp` | 2026-08-17 |

A month later, on 2026-09-16, `HandshakeStress_NoTearNoLoss` still caught a torn
read — its own assertion at `host_tests.cpp:1307`, in a build where ASan
reported no memory error. So the 09-16 occurrence is **not** the race that
076f545 fixed. It is either a residual tear the triple buffer does not close, or
a defect in the test's own invariant.

That distinction is M005/S02's subject, and it is a better-posed question than
the one this milestone started with.

- `format` → exit 0
- 2026-09-19

## M005/S01 — task 2: the race did not reproduce, and here is exactly what was tried

Both outcomes satisfy this slice's definition of done. This is the recorded
non-reproduction, with counts rather than an adjective.

**Locally — Darwin arm64, Apple clang 21 — 120 runs, no failure:**

| Build | Test(s) | Repeats | Result | Wall clock |
|---|---|---|---|---|
| TSan plugin | `HandshakeStress_NoTearNoLoss` + `_TSanClean` | 20 | all pass | 164.9 s |
| ASan plugin | `HandshakeStress_NoTearNoLoss` | 40 | all pass | 72.6 s |
| Release, no sanitizer | `HandshakeStress_NoTearNoLoss` | 60 | all pass | 10.3 s |

**On the filed configuration — ubuntu-24.04, gcc — run `35475629995`,
dispatched against this branch: all five jobs passed.**

That is not evidence of absence. The 2026-09-16 occurrence is one failure in
roughly 34 nights; a single dispatched run has about a 3% prior of catching it,
so a green run is the expected result whether or not the defect is present.
Recording the probability matters more than recording the pass.

**What the attempts do establish:**

- The TSan detector that fired on 22 consecutive nights is now quiet on the
  filed configuration too — consistent with task 3's finding that `076f545`
  fixed that race, and inconsistent with it having merely narrowed.
- The residual tear is **rare**: not reproduced in 120 local runs nor in one
  full CI run. Whatever remains is far less probable than the two-slot race was.
- A local non-reproduction on macOS/clang proves less than the CI one, because
  the filed failures are ubuntu/gcc. Both are recorded; neither is treated as
  absence.

- `format` → exit 0
- `sanitizers` → exit 0 locally (all five variants); CI run `35475629995` → success
- 2026-09-19
