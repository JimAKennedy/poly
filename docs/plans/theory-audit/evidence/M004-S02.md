## M004/S02 — task 1: the clave lane names its approximation

- `format` → exit 0
- `site-unit` → exit 0, 211 tests, 0 failures
- `doc-conformance` → exit 0, 190 tests, 0 failures
- 2026-09-10

The red came from removing S05's `ac-clave-approximation` marker, which was the
only reason the rule passed. Both arms then failed on their own terms: `the
clave lane header reads "Clave" and does not mark itself an approximation`, and
`the patch carries no link to the exact-timeline construction`.

The header now reads `Clave (E(5,16) approximation)`, which sits beside the
theory page's `Clave (timeline, exact)` and makes the pair legible as
approximation and original. The note beneath the patch states the actual
difference — E(5,16) puts its fifth hit at 13 where the clave has 12, which is
the chapter's own prose — and links the section that already carries the
two-step workflow.

The anchor was derived from the heading text rather than trusted from the plan:
`## Son Clave, Rumba Clave, and Their Euclidean Neighbour` slugifies to
`#son-clave-rumba-clave-and-their-euclidean-neighbour`, which is what the link
uses.

Both arms were proved to bite separately, by inverse edit: reverting the header
to `Clave` fails the first arm only; replacing the link with plain text fails
the second only. Testing them together would not have shown that one arm can
carry the case while the other is broken.

The suite's printed suppression count drops from 4 to 3.

## M004/S02 — task 2: the tumbao's onsets are printed

- `format` → exit 0
- `site-unit` → exit 0, 212 tests, 0 failures
- `doc-conformance` → exit 0, 191 tests, 0 failures
- `npm --prefix site run build` → exit 0, run because the printed set uses
  braces and MDX evaluates `{...}` as an expression; inline code keeps them
  literal in the source, which is what the case reads, without breaking the
  build
- 2026-09-10

`theory-afro-cuban`'s tumbao is 16 steps, 6 hits, rotation 14. Rotation 14 is an
unusual spelling and the page asked the reader to take Rule 3 on trust. It now
prints the onsets — `{1, 3, 6, 9, 11, 14}`, nothing on pulse 0, strikes on
bombo (3) and ponche (6).

The case asserts the printed set against the derivation from
`site/src/lib/euclidean-claims.mjs`, never against a literal. That is the whole
point: a hard-coded expectation would let the prose and the triple drift apart
silently, which is the failure this row describes. Watched to fail before the
prose existed — `rotation 14 is an unusual spelling and the page prints no onset
set for it` — and again with one printed number changed: `the page prints onsets
[1,3,6,9,11,15] but E(6,16) at rotation 14 is [1,3,6,9,11,14]`.

## M004/S02 — task 3: F40 corrected, and the slice closed

- `format` → exit 0
- `site-unit` → exit 0, 213 tests, 0 failures
- `doc-conformance` → exit 0, 192 tests, 0 failures
- `e2e` → exit 0, all 13 stages: equivalence (27 chapters), sample equivalence
  (27 chapters), macro-diff (4 cases), first-bar parity, lane mute, control
  audit, console-error gate (46 pages walked)
- `ledger` → exit 0, conforms with the slice `done`: three definition-of-done
  boxes ticked, this evidence file present, F38, F39 and F40 closed, B16
  `accepted`
- 2026-09-10

**The plan's step 1 was wrong and was repaired during execution.** It assumed
F40's premise did not exist, on the strength of the row's `Lands in` naming
`03-afro-cuban.mdx`. The premise is real and lives on `theory-afro-cuban.mdx`,
whose patch carried Cáscara 5%, Conga marcha 10% and Quinto 30% — three lanes
against Rule 5's one, with the construction naming the quinto as that voice.

`ac-theory-one-free-voice` was watched to fail naming all three, the Cáscara and
Conga marcha budgets were zeroed, and the rule was proved to bite by restoring
the conga's 10% — `2 lanes carry a variation budget (Conga marcha 10%, Quinto
(free voice) 30%)`.

`ac-one-free-voice` on Chapter 3's preset is kept as well. It is correct as
written and locks a side that currently complies only because M070 conformed
the factory presets.

Per B16, `e2e` rebuilt `webui/poly_engine.{js,wasm}` and both were restored from
`origin/main` before committing; only explicit paths were staged. The slice's
diff contains no engine artifacts.

No commit SHA is recorded here: this file ships inside the commit it describes.
