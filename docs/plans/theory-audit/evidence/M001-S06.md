# M001/S06 — evidence

## M001/S06 — task 1: Chapter 4 co-attribution and Allen's timing

- `format` → exit 0
- `site-unit` → exit 0, 165 tests, 0 failures (up from 163: cases `S06-F09`
  and `S06-F10`)
- `doc-conformance` → exit 0, 144 tests, 0 failures
- 2026-08-28

Test-first held, though not the way the plan predicted. Both cases were added
before the prose was touched and both failed — on `forbidden`, not on `present`.
`assertClaim` checks forbidden phrases first and throws on the first violation,
so while the uncorrected sentence is still in place the forbidden check fires
before the present check is ever reached. The failure named the exact phrase in
each case, which is stronger evidence of correct wiring than a missing-present
failure would have been.

Mutation proof, run against a copy of `site/` so the working tree could not be
clobbered: restoring `who created its rhythmic vocabulary` fails `S06-F09`
naming that phrase; the copy was discarded rather than reverted.

## M001/S06 — task 2: Chapter 8 attributes Reich's framing to Reich

- `format` → exit 0
- `site-unit` → exit 0, 166 tests, 0 failures (case `S06-F11`)
- `doc-conformance` → exit 0, 145 tests, 0 failures
- 2026-08-28

The repaired step 2 predicted the failure correctly this time: `S06-F11` failed
on its `forbidden` phrase `musicians had known for centuries`, not on a missing
`present` phrase.

`S03-F02` and `S03-F03` also assert against `08-minimalism.mdx` and both still
pass. That matters here rather than being routine: the new paragraph names
*Piano Phase*, and `S03-F02` forbids *Drumming* citations followed closely by
phase-shifting language, so a careless rewrite of this opening could have
tripped it.

Mutation proof, against a copy of `site/`: restoring the "had known for
centuries" clause fails `S06-F11` naming that phrase.

## M001/S06 — task 3: Chapter 9 attributes the swing claim to Linn

- `format` → exit 0
- `site-unit` → exit 0, 167 tests, 0 failures (case `S06-F13`)
- `doc-conformance` → exit 0, 146 tests, 0 failures
- 2026-08-28

Failed first on the `forbidden` phrase, as the repaired step 2 predicted; the
Butler `presentRegex` is unreachable until that phrase is gone, so it was first
exercised in step 4.

Mutation proof, against a copy of `site/`: deleting the Butler cross-reference
fails `S06-F13` with `corrective pattern went missing: /#fr-butler-2006/`. That
is the one this case most needed, because the cross-reference is the half of
the row a prose-only assertion would not have caught.

`ref-36` is deliberately untouched — still one occurrence in the chapter.
Re-tiering it is M002/S06's row F23, and doing it here would put two slices in
the same paragraph.

## M001/S06 — task 4: Chapter 13 hedges the Amen-break superlative

- `format` → exit 0
- `site-unit` → exit 0, 168 tests, 0 failures (case `S06-F14`)
- `doc-conformance` → exit 0, 147 tests, 0 failures
- 2026-08-28

Failed first on the `forbidden` phrase, as the repaired step 2 predicted.

Mutation proof, against a copy of `site/`: restoring `became the most sampled
recording in music history` fails `S06-F14` naming that phrase.

`ref-38` is untouched — the Ethan Hein blog stays as the citation, and
re-tiering it is M002/S06's row F23. This row hedges the claim, not its source.

## M001/S06 — task 5: Chapter 12 cites Monson for Roach's independence

- `format` → exit 0
- `site-unit` → exit 0, 169 tests, 0 failures (case `S06-F15`)
- `doc-conformance` → exit 0, 148 tests, 0 failures
- 2026-08-28

The one task in this slice whose step 2 was already correct before the plan
repair, and it behaved as predicted: `S06-F15` failed on
`corrective pattern went missing: /#fr-monson-1996/`, not on a forbidden
phrase. It carries no `forbidden` entry because nothing in the Roach passage is
wrong — it was simply uncited — so a missing-present failure is the only signal
this case can give. A blanket edit to the step-2 wording would have broken it.

`grep -c 'id="fr-monson-1996"'` on `appendix-references.mdx` returns 1: the
entry already existed and no second one was added.

Mutation proof, against a copy of `site/`: stripping the citation fails
`S06-F15` on the same missing pattern.

## M001/S06 — task 6: mark the lcm translation at all three sites

- `format` → exit 0
- `site-unit` → exit 0, 172 tests, 0 failures (cases `S06-F16-foundations`,
  `S06-F16-gamelan`, `S06-F16-indian`)
- `doc-conformance` → exit 0, 151 tests, 0 failures
- 2026-08-28

Step 2 produced the mixed failure the repaired plan predicted: the two
append-only sites failed on missing `present` caveats, the Indian site on its
`forbidden` phrase `that shared downbeat is sam`.

The first attempt at step 1 would not parse — three `rule` strings were
single-quoted and contained "guide's". Rewritten as concatenated strings with
the apostrophe-bearing fragment double-quoted, matching how the surrounding
claims already handle it. The three cases were absent from the run rather than
failing, which is what exposed it.

Mutation proof, against a copy of `site/`: deleting only the Indian caveat
fails `S06-F16-indian` while `S06-F16-foundations` and `S06-F16-gamelan` still
pass — the three cases are independently wired rather than one assertion in
three costumes.

`S02-F01` and `S02-F12` also assert against `06-indian-classical.mdx` and both
still pass, which the section-heading rename could have disturbed.

## M001/S06 — slice close-out

All seven rows closed and all eight definition-of-done items met. `ledger`
conforms with the slice `done`, which is the stricter claim: every box ticked,
this evidence file on disk, no row left open.
