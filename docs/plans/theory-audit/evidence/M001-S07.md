## M001/S07 — tasks 1 and 2: migrate the completeness gate to the ledger

Committed together: the two tasks split one file's migration, and the file has
no green intermediate state — after task 1 alone, `doc-conformance` fails the
two Related-issues cases by design. The smallest unit that passes its own gate
is both tasks.

- `format` → exit 0
- `site-unit` → exit 0, 163 tests, 0 failures
- `doc-conformance` → exit 0, 142 tests, 0 failures (11 in this file, up from
  10: the F01–F54 case cannot see a malformed `H`-prefixed ID, so that assertion
  is its own case)
- `doc-discipline` → exit 0, ledger conforms, 68 docs carry a valid class

The gate bites — four ledger mutations, each tripping one named case, each
reverted and confirmed by `git diff --quiet`:

- delete the `F31` row → case 2 fails, `missing finding IDs: F31`
- `H03` status → `pending` → case 6 fails, `H03: status token missing or
  invalid (got "pending"; …)`
- `#91` bullet cites `F99` → case 9 fails, `#91: entry cites F99, which is not
  a row in this ledger`
- `Out of scope` cites `#999` → case 11 fails, `Out of scope names issue(s)
  absent from the Related issues list: #999`

Case 10 ("no issue is both listed and declared deliberately absent") passes
vacuously: the ledger carries no absent-prose, so nothing can collide. Retained
and documented in the file rather than deleted — it bites the day one is added.

- commit: this file ships in the commit it describes, so its SHA is
  recorded by the next task's append rather than fabricated here. The
  commit carries `Slice: M001/S07` and `Rows: H03`.
- 2026-08-28

## M001/S07 — task 3: archive the plan doc and repoint its neighbours

- `format` → exit 0 (first run applied an end-of-file fix to
  `docs-drift-map.yml`, whose last entry this task removed; clean on re-run)
- `site-unit` → exit 0, 163 tests, 0 failures
- `doc-conformance` → exit 0, 142 tests, 0 failures
- `doc-discipline` → exit 0; `doc-taxonomy` accepts the `archived` class across
  68 docs, and `doc-completeness` reports 59 mapped-or-declared with 8 exempt by
  class — one more than before, which is the drift-map declaration becoming
  redundant rather than merely deleted
- 2026-08-28

The plan doc is `class: archived` and names the ledger as its successor. It is
kept rather than deleted because its tables carry the "Audit §" column mapping
each finding to a section of `poly_theory_audit.md`, which the ledger's rows do
not. `poly_theory_audit.md`'s own archival note pointed at the plan doc as "the
live record"; it now points at the ledger, which is the staleness H04 names.
