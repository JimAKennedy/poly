## M006/S01 — task 1: the six re-points whose sources were already here

- `format` → exit 0
- `site-unit` → exit 0, 229 tests, 0 failures
- `doc-conformance` → exit 0, 208 tests, 0 failures
- `npm --prefix site run build` → exit 0
- 2026-09-11

Seven markers cleared across six files, closing B01–B05 and B17. Three remain —
two for B06 and one for B07 — which is what tasks 2 and 3 own.

**The upgrade pattern is M002's own, and the case asserts both halves of it.**
The guide's grammar makes a `<sup>[N]</sup>` a named-theory claim and a plain
link a bibliographic listing, so moving a claim onto a Tier-A Further Reading
source means removing the superscript *and* adding an inline author-year link.
Removing the superscript alone would take the claim out of the tier check's
scope rather than source it — an evasion that looks exactly like a fix. That is
why `S02-F18` in `citation-tier.test.mjs` asserts the old superscript is gone
and the new anchor present, and why this slice follows suit.

Two re-points stayed superscripts because their replacement is itself a numbered
Tier-A reference: B01's "connects to drumming traditions" half moves from
`ref-3` to `ref-1`, and the appendix's E(5,16) gap sequence moves from `ref-42`
to `ref-1`. Both are Toussaint's result, which the markers said outright.

**The case is keyed on files, not on row ids in the marker text.** The first
version matched `M006/(B\d{2})` and found five markers instead of seven: the two
`ref-42` markers predate B17 and say "No M006 row owns ref-42 yet", so a row-id
match silently missed exactly the pair the row was created for. Keying on the
six files this task clears finds all seven.

One scripted removal failed its own assertion and left its file untouched rather
than half-edited — the `appendix-euclidean-reference.mdx` marker names
`ref-1`, not `ref-42`, so the hint did not match. It was completed separately.

Watched to fail with all seven present, and again with one marker restored —
`05-gamelan.mdx (1)` — removed by inverse edit.
