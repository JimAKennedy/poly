## M002/S04 — task 1: Chapter 6, source the tala, theka and layakari claims

- `format` → exit 0
- `site-unit` → exit 0, 178 tests, 0 failures (1 new case, `S04-F20`)
- `doc-conformance` → exit 0, 151 tests, 0 failures
- 2026-09-07

The chapter cited none of Clayton, Nelson or Kippen before this task, so both
`presentRegex` arms were unsatisfied and each is a real lock rather than a
standing guard. `S04-F20` failed on the forbidden side first, as the plan
required.

The theka claim was uncited rather than mis-cited, so satisfying that half of the
definition of done meant adding Kippen (1988) to the sentence M001/S02 wrote when
it corrected F12 — not re-pointing an existing citation.

`ref-25` was a "Konnakol Mastery" video cited for *layakari*. Konnakol is
Carnatic vocal percussion, not rhythmic augmentation ratios, so it was off-topic
as well as low-tier — the same shape as `ref-17` in M002/S03.

Three M001 locks live in this file and `site-unit` was run immediately after the
edits rather than at the end of the task. `S02-F01`, `S02-F12` and
`S06-F16-indian` all passed. Note the plan named the third as `S06-F16`; the
case is actually registered per-site as `S06-F16-foundations`,
`S06-F16-gamelan` and `S06-F16-indian`, and it is the `-indian` one that guards
this chapter.

`06-indian-classical.mdx` now contains no `#ref-21`–`#ref-25` at all. The
remaining claim citations of those references are on the companion page, which
task 2 takes.

## M002/S04 — task 2: companion page, drop the video citations from two claims

- `site-unit` → exit 0, 179 tests, 0 failures (2 new cases across the slice)
- `doc-conformance` → exit 0, 151 tests, 0 failures
- `format` → exit 0
- `ledger` → exit 0, conforms with the slice `done`: both definition-of-done
  boxes ticked, this file on disk, and F20 closed
- 2026-09-07

`S04-F20-theory` failed on the forbidden side before the edit. Its
`presentRegex` arm could not have failed — the page already cites Clayton — so
it is a standing guard against a later edit stripping Clayton out, not a lock
this slice established.

The generalisation earned itself, proved rather than asserted. Re-inserting the
multi-reference block `<sup>[23](…#ref-23), [24](…#ref-24)</sup>` failed
`S04-F20-theory`; the same block tested against the single-reference pattern
M002/S02 and M002/S03 used does **not** match it. So the narrower form would
have reported this page clean while two named-theory claims still cited a
journal PDF and a pubpub article. The mutation was reverted with an inverse
edit rather than `git checkout --`, per the note M002/S03 left after losing its
work that way.

Line 13 needed no replacement citation: the Clayton reference already in that
sentence carries the tala definition, so the superscript was simply removed.
Line 25's layakari-ratios rule had no other source and gained Clayton, the
scholarly standard on laya and metre.

The Sources `See also refs [21]–[25]` listing was verified intact after the
edits. It is a plain-link bibliographic pointer with no `<sup>` around it, which
is why the forbidden pattern cannot reach it.
