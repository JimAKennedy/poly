## M005/S04 — task 1: Silverman at the svatbarska passage

- `format` → exit 0
- `site-unit` → exit 0, 223 tests, 0 failures
- `doc-conformance` → exit 0
- `npm --prefix site run build` → exit 0
- 2026-09-10

Silverman goes beside the existing Rice citation rather than replacing it: Rice
is cited for the tempos, Silverman for the tradition's own history — its
condemnation under state socialism, its post-1989 decline against chalga. The
added sentence says that, and draws the consequence the chapter can use: the
virtuosity it goes on to describe belongs to a competitive scene rather than a
preserved repertoire.

Watched to fail before the citation existed, and again with it removed —
`fr-silverman-2007 is not cited near "svatbarska muzika"` — restored by inverse
edit.

F49 does not close here. Its second half, Peycheva & Dimov, is task 2.

## M005/S04 — task 2: the source nobody here can read

- `format` → exit 0
- `site-unit` → exit 0, 224 tests, 0 failures
- `doc-conformance` → exit 0, 203 tests, 0 failures
- `npm --prefix site run build` → exit 0
- `ledger` → exit 0, conforms with the slice `done`
- 2026-09-11

**This task's planned halt fired, and the plan's own step 4 is what caught it.**
It says to add Peycheva & Dimov and cite them where Chapter 7 touches Romani
musicianship, and then: *"If the chapter has no such passage, stop and report
rather than writing one to hang a citation on."* Chapter 7 has no such
passage — zero occurrences of Roma, Romani or Gypsy.

`theory-balkan.mdx` was checked next and has no home either. Its Rule 7 is about
wedding-band ensemble tightness and its Attribution line already assigns that
claim to Rice (1994); nothing on the page discusses zurna playing or Romani
musicianship. Adding Peycheva & Dimov to Rule 7's attribution would have
asserted that their book supports a claim nobody here has confirmed it supports.

So the entry is added and cites nothing, which the row records. Two things make
that defensible rather than sloppy:

- **The entry states its own limits.** Existence, authors, year, title and
  series were confirmed from catalogue records; the contents were not, the work
  being in Bulgarian with no translation obtained. It says so in the fixed
  phrase `contents unverified`, so `grep -rn "contents unverified" site/`
  enumerates every entry in that state — one today. A tier value cannot carry
  that: tier B is a judgement about a work's standing, not about our ability to
  read it.
- **It is a deliberate orphan, and M006 needs to know.** M006/S02's B08 covers
  entries cited by no page, and this adds one on purpose. F49's `Item` says so,
  so the burn-down does not read it as rot and delete a source that was added
  advisedly.

The case was watched to fail twice — with the entry missing, and with the phrase
replaced by "fully checked" — and restored by inverse edit each time.

No commit SHA is recorded here: this file ships inside the commit it describes.
