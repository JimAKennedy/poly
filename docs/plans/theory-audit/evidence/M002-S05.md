## M002/S05 — task 1: Chapter 7, cite Brăiloiu for aksak and Rice for svatbarska

- `site-unit` → exit 0, 180 tests, 0 failures (1 new case, `S05-F21`)
- `doc-conformance` → exit 0, 151 tests, 0 failures
- `format` → exit 0
- `ledger` → exit 0, conforms with the slice `done`: all three
  definition-of-done boxes ticked, this file on disk, and F21 closed
- 2026-09-07

`S05-F21` failed on the forbidden side first. Both `presentRegex` arms were
unsatisfied beforehand — the chapter cited only `fr-goldberg-2015` — so each is
a real lock this slice established rather than a standing guard.

The third definition-of-done item asks specifically that M001/S04's long-beat
lock survives, so it was asserted by name rather than inferred from a green
suite: `✔ S04-F06 (07-balkan.mdx)`. Its subject is line 41, which sits between
the two lines this task edited. `S04-F04`, `S04-F05` and the `S04-F05`
arithmetic case — M001/S04's other locks on this chapter — passed too.

The lock bites: re-inserting `<sup>[26](/appendix-references/#ref-26)</sup>`
failed `S05-F21` on the forbidden pattern. Reverted with an inverse edit rather
than `git checkout --`, per the note M002/S03 left after losing its work that
way.

`07-balkan.mdx` now contains no `#ref-26` or `#ref-27`. `theory-balkan.mdx` was
verified untouched (`git diff --quiet`) and still cites `ref-26` once in its
`Primary:` Sources line — a plain link with no `<sup>`, which is both why the
forbidden pattern cannot reach it and what stops the two references orphaning
now that their claims have moved.

Goldberg (2015) was deliberately not moved. It is the third source the
definition of done offers, but it is already cited at the long-beat timing claim
and is the authority for neither the aksak definition nor svatbarska muzika.
