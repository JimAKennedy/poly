## M002/S06 — task 1: tier all 99 entries and assert every entry declares one

- `format` → exit 0
- `site-unit` → exit 0, 181 tests, 0 failures (1 new case, `S06-tiers-declared`)
- `doc-conformance` → exit 0, 151 tests, 0 failures
- `doc-discipline` → exit 0
- 2026-09-07

Tier distribution across 99 entries: **A 68, B 13, C 18**. The audit
(`docs/audits/poly_theory_audit.md` §4) names about half explicitly and those
follow it — including Uribe (1996) and Mauleón (1993), which are method books
the audit nonetheless places in Tier A.

Judgement calls not covered by the audit, recorded because a later reader will
want to know they were decisions rather than transcription:

- `fr-riley-1994` (*The Art of Bop Drumming*, Manhattan Music) → **B**. A
  standard pedagogical text, not scholarship.
- `fr-reynolds-1998` (*Energy Flash*, Picador) → **B**. Trade history, not
  peer-reviewed.
- `ref-9` (Oluranti, University of Pittsburgh dissertation) → **A**. A doctoral
  dissertation is scholarship.
- `ref-34` (Reich, "Music as a Gradual Process") and `ref-46` (Bjorklund's SNS
  technical note) → **A** as primary sources, despite informal venues.
- `ref-8` (Scribd upload) and `ref-30` (Academia.edu) → **C** and **B**: a
  self-hosted preprint by a named author is not the same thing as an anonymous
  document upload.

**`ref-36` was decided by reading the page, as the plan required, and the answer
inverted the expectation.** The design doc allowed that it might tier **A** as
a primary source, being titled "Roger Linn on Drum Machine Groove". It is not:
it is Thomas Brett's own commentary on his blog, quoting Linn from an *Attack
Magazine* interview published elsewhere. So it is **C**, and the real primary
source — the Attack Magazine interview — is the upgrade target M006/B06 should
name. Had the count been trusted instead of the page, this would have been
tiered A and the claim left citing a blogger's paraphrase.

**The suppression set task 2 faces is larger than the design predicted:
8 references across 10 sites, not 7 across 8.** The extra is `ref-42`
(Schloss, University of Victoria), which the audit itself calls Tier B —
"university-hosted, legitimate" — cited at `10-brazilian.mdx:19` and
`appendix-euclidean-reference.mdx:145`. M006/S01 carries rows B01–B07 for seven
sources, so it is one row short. That is M006's to correct, not this slice's.

The assertion was proved non-vacuous rather than trusted for being green:
removing `data-tier` from `ref-5` fails it with `ref-5 (no data-tier)`, and
setting `data-tier="X"` fails it with `ref-5 (data-tier="X")`. Both reverted
with inverse edits.
