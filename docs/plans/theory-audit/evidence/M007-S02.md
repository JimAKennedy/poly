## M007/S02 — task 1: the breakbeat kick clears the snare

- `format` → exit 0
- `site-unit` → exit 0, 235 tests, 0 failures
- `doc-conformance` → exit 0
- `npm --prefix site run build` → exit 0
- 2026-09-12

M004/S05 found this on the first rule it newly checked: the chopped kick was
E(5,16) at rotation 3, onsets `{3,6,9,12,15}`, against a backbeat snare on
`{4,12}`. Pulse 12 was shared, so the kick landed in a snare slot on a page
whose Rule 7 has it avoid them. It was marked rather than fixed there, because
the choice between moving the kick and qualifying the rule is a musical one.

**Rotation 13**, onsets `{0,3,6,9,13}`, re-derived here with `bjorklund` and
`rotate` rather than taken from the plan. Six rotations clear both slots; r13 is
the one that keeps an onset on pulse 0, so the pattern still starts on the
downbeat a jungle kick usually does, and four of its five onsets are where they
were. Rule 7 is unchanged — the page stops contradicting itself rather than
having its rule relaxed to fit.

Qualifying Rule 7 was the alternative and was rejected. M004/S03 did weaken
gamelan Rule 4, but only because Poly's Kotekan L-mode genuinely cannot express
the strict reading. Here the engine can express it and the fix is one cell.

The red came from removing the marker: `kick onsets [3,6,9,12,15] intersect
snare slots [4,12] at [12]`. Proved to still bite by restoring rotation 3 and
watching the same failure, then restoring 13 by inverse edit.

**`grep -rn patch-divergence-ok site/` now returns nothing**, and the suite
prints `0 live suppression(s)`. Every marker M004/S05 created is discharged:
five were handoffs removed by the slices that owed them, and this was the sixth.

## M007/S02 — task 2, theory-afrobeat: five rules

- `format` → exit 0
- `site-unit` → exit 0, 240 tests, 0 failures
- `doc-conformance` → exit 0
- 2026-09-12

The page had **no coverage at all** before this — five checkable rules, none
asserted. All five pass, which is the expected outcome for a patch its own page
was written around; the value is that a later edit now cannot quietly break them.

| Rule | Case | What it settles |
|---|---|---|
| 1 | `afb-bell-unvaried` | a 12-step bell carrying no mutation |
| 3 | `afb-kick-sparse-fixed` | three to four kick hits, no mutation |
| 4 | `afb-hat-continuous` | the hat strikes at least three quarters of its grid |
| 5 | `afb-snare-whispers` | the snare sits below the bell in velocity |
| 7 | `afb-core-unvaried` | bell, kick and hat carry no mutation |

Each was mutated and watched to fail, then restored by inverse edit: bell 12→16
steps, kick 3→7 hits, hat 14→4 hits, snare velocity 60→120, bell mutation
0%→15%.

**Rule 4 is checked in half, and the case says so.** "Continuous but dynamically
contoured" has two claims: continuity, which the hit count settles, and an
accent pattern forming an independent voice, which no cell reports. The
predicate checks the first and its comment records that it does not check the
second — a half-checked rule that admits it is honest; one that implies full
coverage is not.

**A registration-order bug was caught before commit.** The first version
appended `CHECKLIST.push(...)` at the end of the file, after the loop that
registers a test per rule had already run. All five cases were silently
never registered — the suite went from 26 tests to 26 and passed. Moving the
block above the loop registered them. A rule that is not registered is
indistinguishable from a rule that passes, which is precisely the failure mode
this milestone exists to remove, and it nearly shipped inside the fix for it.
