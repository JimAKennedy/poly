
## M003/S05 — task 1: cite Chapter 5's cyclic-time opening

- `format` → exit 0
- `site-unit` → exit 0, 196 tests, 0 failures
- `doc-conformance` → exit 0, 175 tests, 0 failures (one more than task 0's 174:
  the new `S05-F35` case)
- 2026-09-09

The case was watched to fail before the prose changed — `corrective pattern went
missing: /time is not a line[\s\S]{0,400}#fr-tenzer-2000/` — so the bounded span
is proved to be doing the work rather than matching Vitale's citation further
down the page.

Tenzer, not Geertz. The audit attributes the framing's origin to Geertz's
*Negara* (1980), but that work has no appendix entry, and adding one is M006's
scope. `fr-tenzer-2000` was already present at tier A and already cited twice
from `theory-gamelan.mdx`; chapters may cite `fr-` anchors, as seven already do.

`site-unit` resolves to `npm --prefix site test`. This plan's Validation table
names `node --test site/tests/`, which collects only one file from the repo
root; the plan's own instruction to prefer `.jk/validations.yml` was followed.

## M003/S05 — task 2: Gamelan Rule 5's third part

- `format` → exit 0
- `site-unit` → exit 0, 197 tests, 0 failures
- `doc-conformance` → exit 0, 176 tests, 0 failures

Both arms of `S05-F33` were watched to fail, neither assumed:

- before the prose changed → `corrective pattern went missing:
  /third part[\s\S]{0,300}pokok/`, so the `present: ['pokok']` arm, which Rule 6
  already satisfies, is not what carries the case
- with `kotekan polos` reinserted into the new sentence → `forbidden phrase
  reappeared: "kotekan polos"`, restored by inverse edit to a one-line diff

Rule 5 gains one sentence and no citation. The practice is not new scholarship:
Rule 6 makes pokok what the interlock elaborates, Construction step 2 builds a
Pokok lane, and the page's patch carries it as Lane 5. The cross-reference is
plain text, `(Rule 6)`, because the page defines no rule anchors.

The regex is `third part`, not `third (?:part|lane)`. Construction step 4 says
"a sparse third lane" and the patch names a "Pokok melody" lane, so the `lane`
alternative would have matched prose that predates this slice and the case would
have passed without Rule 5 changing.

Plan residue, not acted on: the sample `rule:` string in Task 2 step 1 still
reads "cited and linked to Rule 6". Step 3, the definition of done and the F33
row all say the sentence carries no citation, so the intent was unambiguous and
the rule string was written to match them.
- 2026-09-09

## M003/S05 — task 3: the Rachenitsa Note column and stand-in line

- `format` → exit 0
- `site-unit` → exit 0, 198 tests, 0 failures
- `doc-conformance` → exit 0, 177 tests, 0 failures
- `preset-table-conformance` → exit 0, 17 tests, 0 failures, run explicitly to
  confirm this task did not perturb it; it guards `appendix-presets.mdx`, not
  this page

The oracle was watched to fail twice, not assumed:

- before the column existed → `the Rachenitsa table has no "Note" column, so a
  reader cannot tell what any lane sounds like on a GM kit`
- with lane 3's cell changed 76 → 77 → `Rachenitsa lane 3 Note cell is "77" but
  presets.json says 76`, restored by inverse edit

The case reads `site/src/generated/presets.json`, not the
`site/public/webui/presets.json` the plan names. The two files are byte-identical
copies, and `src/generated` is the one `preset-table-conformance.test.mjs`
imports, so the lock follows the suite the repo already treats as the oracle.

The `Note` header sits between `Subdivision` and `Velocity` and carries bare
note numbers, matching what `PresetTable` emits for that column. If
`07-balkan.mdx` is ever migrated off the hand-written `PolyPatch` table, the
column is a drop-in rather than a conflict.

Two arms of the case are weaker than they look and are recorded rather than
hidden. The prose window is bounded to the region between the table and the next
heading, because an end-of-file window would let a mention anywhere later in the
chapter satisfy it. Within that window the `roleLabel` arm for `rim` is
pre-satisfied by the `PolyPreviewCard` caption, which already reads "tupan, rim,
kaval, and gadulka"; the `kick`, `woodblock` and `hat` arms are what drive the
assertion, along with the two `no GM drum` arms.
- 2026-09-09
