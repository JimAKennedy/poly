## M003/S01 — task 1: the About page, its scope exclusions, and the lock host

- `format` → exit 0
- `site-unit` → exit 0, 184 tests, 0 failures (1 new case, `S01-F36`)
- `doc-conformance` → exit 0, 163 tests, 0 failures — up from 162, the increase
  being the new `scope-framing.test.mjs` host now running inside the runner
- `doc-discipline` → exit 0
- 2026-09-09

The repositioning statement was **extracted from the audit, not retyped**: a
script reads `docs/audits/poly_theory_audit.md` between the "Proposed
repositioning statement" and "What this frame lets you preserve" headings,
strips the blockquote markers, and asserts it found exactly four paragraphs
whose first begins "This guide is not a comprehensive review". Transcribing 249
words by hand is how a verbatim quotation stops being verbatim.

`S01-F36` failed first with `ENOENT` on `about-this-guide.mdx` — the
file-not-found form the plan predicted, rather than an assertion failure, since
the page did not exist yet.

**The untracked-file trap caught this task and was caught in turn.** After
adding the drift-map entry, `doc-completeness` still read `59 doc(s)` and
`doc-taxonomy` still read `68` — the new page was untracked, and jk-standards
does not enumerate untracked files under a doc root. Staging first moved them to
`60` and `69`. This is jk-standards#96, filed earlier in this session after the
same trap let 211 violations through; the plan's instruction to validate after
`git add` is what made it visible here rather than at push time.

`REQUIRED` was extended before the runner, deliberately, and the wiring test
failed by name — `check-doc-conformance.sh dropped required guardrail:
site/tests/scope-framing.test.mjs`. That failure is the evidence the guard
defends the new host rather than merely passing beside it. The runner now names
18 guardrail files where it named 17.

The exclusions section links to `/06-indian-classical/` and
`/theory-counterpoint-overview/` as **pages**, never to anchors. M003/S02–S05
have not landed and the headings they will add do not exist, so an anchor naming
one would be the forward reference M002 spent six slices removing.

## M003/S01 — task 2: reachability from the introduction and all twelve theory pages

- `site-unit` → exit 0, 185 tests, 0 failures (1 new case, `S01-F24`)
- `doc-conformance` → exit 0, 164 tests, 0 failures
- `format` → exit 0
- `ledger` → exit 0, conforms with the slice `done`: all three
  definition-of-done boxes ticked, this file on disk, F24 and F36 closed
- 2026-09-09

`S01-F24` failed first at `13 of 13 page(s) do not link to /about-this-guide/`,
which also confirmed the glob had found all twelve theory pages before any edit
— a glob matching nothing would have failed the count assertion instead, and
that is the failure mode the count exists to catch.

Eleven of the twelve theory pages share an italic preamble ending in the same
sentence, verified as exactly eleven occurrences of `not original theory.*`
before the bulk edit rather than assumed from a sample. The twelfth,
`theory-counterpoint-overview.mdx`, has no preamble and instead links **upward**
from its own "None of this is original research" paragraph, which is left
intact. That paragraph and the About page now agree by reference rather than by
copy — two paragraphs saying the same thing in slightly different words is how a
guide starts contradicting itself.

The lock names the offending file rather than a count: removing the link from
`theory-balkan.mdx` fails with `1 of 13 page(s) do not link to
/about-this-guide/: theory-balkan.mdx`. Reverted with an inverse edit, not
`git checkout --`, since the file carried this task's uncommitted work.

Thirteen near-identical edits is exactly where one gets missed, which is why the
case reports every missing file rather than stopping at the first.
