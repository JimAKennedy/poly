## M002/S02 — task 1: Chapter 3, re-cite the clave-matrix and gap claims

- `format` → exit 0
- `site-unit` → exit 0, 175 tests, 0 failures (1 new case, `S02-F18`)
- `doc-conformance` → exit 0, 151 tests, 0 failures
- 2026-09-04

`S02-F18` failed on the forbidden side before the edit —
`forbidden pattern reappeared: /<sup>\[10\]\(\/appendix-references\/#ref-10\)<\/sup>/`
— which is the failure the plan required to see first.

Two sources, not one, because line 79 makes two claims. The son clave's gap
sequence is Peñalosa's; that `E(k,n)` can only generate gaps of two consecutive
values is Toussaint's (`ref-1`). Citing Peñalosa for the second would have
replaced a bad citation with a wrong one, which is the defect class this
milestone removes.

`site-unit` was run immediately after the line 79 edit rather than at the end of
the task, because four other suites read this file — `chapter-euclidean-guardrail`,
`idiom-break-framing`, `prose-pattern-claims`, `prose-conformance-claims` — and
line 79 is inside the Euclidean guardrail's subject matter. None objected.

`03-afro-cuban.mdx` now contains no `#ref-10` at all. The remaining claim
citation of that reference is on the companion page, which task 2 takes; the
`See also refs [10]–[13]` listing in the companion's Sources section stays, as a
bibliographic pointer rather than a named-theory claim.

## M002/S02 — task 2: companion page, drop the video citation from the clave claim

- `site-unit` → exit 0, 176 tests, 0 failures (2 new cases across the slice)
- `doc-conformance` → exit 0, 151 tests, 0 failures
- `format` → exit 0
- `ledger` → exit 0, conforms with the slice `done`: both definition-of-done
  boxes ticked, this file on disk, and F18 closed
- 2026-09-04

`S02-F18-theory` failed on the forbidden side before the edit. Its
`presentRegex` arm could not have failed — the page already carried four
`fr-penalosa-2009` citations — so it is a standing guard against a later edit
stripping Peñalosa out, not a lock this slice established. The plan says so, so
the executor is not left wondering why only one arm can bite.

The two cases are independent, proved rather than assumed: re-inserting
`<sup>[10](/appendix-references/#ref-10)</sup>` into `03-afro-cuban.mdx` failed
`S02-F18` while `S02-F18-theory` still passed. Reverted and confirmed with
`git diff --quiet`. One guard counted twice would have failed both.

The companion's Sources line still reads `See also refs [10]–[13]`, verified
after the edit. That is deliberate: it is a bibliographic pointer, not a
named-theory claim, so it satisfies the definition of done as written, and
keeping it stops ref-10 becoming an orphan the moment its claims moved away.
