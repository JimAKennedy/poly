## M002/S02 — task 1: Chapter 3, re-cite the clave-matrix and gap claims

- `format` → exit 0
- `site-unit` → exit 0, 175 tests, 0 failures (1 new case, `S02-F18`)
- `doc-conformance` → exit 0, 151 tests, 0 failures
- 2026-09-04

`S02-F18` failed on the forbidden side before the edit —
`forbidden pattern reappeared: /<sup>\[10\]\(\/appendix-references\/#ref-10\)<\/sup>/`
— which is the failure the plan required to see first.

Two sources, not one, because line 79 makes two claims. The son clave's gap
sequence is Peñalosa's; that `E(k,n)` can only generate gaps of two consecutive
values is Toussaint's (`ref-1`). Citing Peñalosa for the second would have
replaced a bad citation with a wrong one, which is the defect class this
milestone removes.

`site-unit` was run immediately after the line 79 edit rather than at the end of
the task, because four other suites read this file — `chapter-euclidean-guardrail`,
`idiom-break-framing`, `prose-pattern-claims`, `prose-conformance-claims` — and
line 79 is inside the Euclidean guardrail's subject matter. None objected.

`03-afro-cuban.mdx` now contains no `#ref-10` at all. The remaining claim
citation of that reference is on the companion page, which task 2 takes; the
`See also refs [10]–[13]` listing in the companion's Sources section stays, as a
bibliographic pointer rather than a named-theory claim.
