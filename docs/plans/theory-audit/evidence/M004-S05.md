## M004/S05 — task 1: reach every patch in a file

- `format` → exit 0
- `site-unit` → exit 0, 199 tests, 0 failures
- `doc-conformance` → exit 0, 178 tests, 0 failures (one more: `S05-parse-by-title`)
- 2026-09-10

`parsePolyPatch(src, title)` — `title` optional, defaulting to the previous
behaviour of taking the first `<PolyPatch>` in the file. The nine review-defect
tests call it without a title and were not edited; all nine still pass, which is
the evidence the default path is preserved rather than merely asserted.

The case was watched to fail first, and on the right assertion: `selecting
"Javanese Colotomic Nesting" returned the same lanes as "Balinese Kotekan
Interlocking" — the title is being ignored`. It reads both titles out of the
file rather than transcribing lane data, so it keeps working if either patch is
renamed or re-lane'd, and fails if a chapter drops to one patch.

A duplicated `if (!inBlock) continue;` left by the edit was removed before
committing; the guard is now stated once, above the closing-tag break.

## M004/S05 — task 2: the checklist and the divergence marker

- `format` → exit 0
- `site-unit` → exit 0, 205 tests, 0 failures
- `doc-conformance` → exit 0, 184 tests, 0 failures
- 2026-09-10

Five rules seeded on the two theory pages that carried no assertion at all.
Four pass; one does not, and that is the substantive result of this task.

**`theory-electronic-breakbeat` contradicts its own Rule 7.** The chopped kick
is E(5,16) at rotation 3 — onsets {3,6,9,12,15} on a 16-pulse grid — and the
backbeat snare is E(2,4) at rotation 1, {4,12}. Pulse 12 is shared, so the kick
lands in a snare slot on a page whose Rule 7 is "the kick syncopates against the
snare, avoiding its slots". Derived with the repo's own `bjorklund` and `rotate`
from `site/src/lib/euclidean-claims.mjs`, not by hand. Suppressed with a
`patch-divergence-ok` marker carrying the untriaged reason, and opened as ledger
row B15 under M007/S02. The fix is a rotation that clears the snare or a
qualification to Rule 7, and that is a musical decision for a human.

Worth stating plainly: the 2026-07-30 review found nine such contradictions by
hand and all nine are fixed. The first newly-checked rule found a tenth. That is
evidence the class is live rather than exhausted, which is the argument M007
rests on.

Each of the four passing rules was mutated and watched to fail, then restored by
inverse edit — `git status` clean on `site/src/content/docs/` after each:

- `min-one-variable` — phasing voice Drift +0.25 → 0 → fails, `lanes carry
  non-zero Drift (none)`
- `min-voices-flat` — phasing voice Velocity 85 → 70 → fails
- `min-deterministic` — fixed voice Mutation 0% → 5% → fails
- `ebb-anchor-immutable` — snare Mutation 0% → 8% → fails

The marker was proved to be **narrow** and **self-cleaning**, which is the
second definition-of-done item:

- with the `ebb-kick-avoids-snare` marker live, breaking `ebb-anchor-immutable`
  on the same page still fails — the marker excuses one rule, not the page
- a marker placed on a rule that *passes* fails its own case: `but the rule
  passes — remove the marker rather than leaving a suppression nobody needs`

The live suppression count is printed by the suite: `patch-divergence-ok: 1 live
suppression(s)`.

**TDD deviation, recorded rather than glossed.** The plan's step 2 says to run
the five cases and watch them fail on the machinery not existing. The checklist
data and its iteration were written together instead, so four cases passed on
first run and one failed on its rule. No case was observed failing for lack of
the mechanism. What establishes non-vacuity here is the mutation round above —
every rule watched to fail on its own terms — not the order the code was
written in. The plan's step-2 prediction that "those rules hold today" was also
wrong for one of the five.

## M004/S05 — task 3: the chapter entries

- `format` → exit 0
- `site-unit` → exit 0, 211 tests, 0 failures
- `doc-conformance` → exit 0, 190 tests, 0 failures
- 2026-09-10

Six rules added across the three chapter patches. **No patch was changed** —
correcting them is S01, S02 and S03's work.

Run before any marker was added, the split was exactly what the plan predicted:
five fail, `ac-one-free-voice` passes. The plan made that a prediction to check
rather than a fact to assume, with instructions to stop if `ac-one-free-voice`
failed, because that would mean the preset had moved since planning and F40's
amendment needed revisiting. It passed, so F40's amendment stands.

The five failing rules carry a `patch-divergence-ok` marker naming the row that
owns the gap — F37 for the two `ssa-` rules, F38 for `ac-clave-approximation`,
F41 for the two `gam-` rules. These are handoffs, not divergences: **each
sibling slice must remove its own marker as part of making its rule pass**, and
a marker left standing after its row closes is the rot M007/S03 exists to catch.

`gam-structural-overlap` is worth reading before S03 plans against it. Sangsih
carries `Kotekan L1`, the strict complement of polos, so the pair intersect
nowhere *by construction* — polos occupies steps {0,2,3,5,6} and its complement
is {1,4,7}. theory-gamelan Rule 4 calls an everywhere-empty intersection
mechanical, so the rule cannot be satisfied while strict L-mode stands: S03's
fix has to change the mode or add accent doubling, not adjust a triple. The
predicate says so in its failure message rather than leaving it to be derived.

`ac-one-free-voice` carries no marker and is the one entry here proved to fail
on its own terms: raising lanes 4 and 5 of `Cuban Son Montuno` to
`mutationRate` 0.1 fails with `2 lanes carry a mutation budget (conga, shaker)`.
Restored with `git checkout HEAD -- site/src/generated/presets.json`, which was
safe here only because that file carried no uncommitted work — verified clean
after. The other five rules were already proved to fail; that is why they are
marked.

The live suppression count is printed and now reads `patch-divergence-ok: 6
live suppression(s)` — five handoffs plus B15's untriaged finding from task 2.
