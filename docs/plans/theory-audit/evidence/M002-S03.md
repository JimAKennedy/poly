## M002/S03 — task 1: Chapter 4, source the opening and phrase claims, drop the rest

- `site-unit` → exit 0, 177 tests, 0 failures (1 new case, `S03-F19`)
- `doc-conformance` → exit 0, 151 tests, 0 failures
- `format` → exit 0
- `ledger` → exit 0, conforms with the slice `done`: both definition-of-done
  boxes ticked, this file on disk, and F19 closed
- 2026-09-04

Chapter 4 carried two of the four flagged references, not four. `ref-14` sat on
an opening sentence that already cited Allen & Veal — M001/S06 put it there — so
the fix was dropping the superscript and adding nothing. `ref-17` sat on a
sentence describing Poly's lane behaviour rather than Afrobeat, and its citation
was removed with no replacement. Refs [15] and [16] carried no claim anywhere.

The definition of done's phrase-gating claim turned out to be uncited
altogether, so satisfying it meant adding Veal (2000) rather than re-pointing an
existing citation.

`S03-F19` failed on the forbidden side first, on the `ref-14` pattern.
`#fr-veal-2000` was absent from the chapter, so that arm is a real lock this
slice established; `#fr-allen-veal-2013` was already present and is a standing
guard against a later edit stripping it out.

M001/S06's `S06-F09` locks the sentence the line 13 edit touched. `site-unit`
was run immediately after that edit rather than at the end of the task, and
`S06-F09` and `S06-F10` both passed. A slice that quietly broke a previous
milestone's lock would have been the worst outcome available here.

The lock bites on its *second* forbidden pattern, not merely its first:
re-inserting `<sup>[17](/appendix-references/#ref-17)</sup>` failed `S03-F19`
naming the ref-17 regex specifically.

Method note for later slices: that mutation was reverted with an inverse edit,
not `git checkout --`. The file still held this task's uncommitted work, so a
checkout would have discarded the task along with the mutation — which is
exactly what happened on the first attempt, and cost a re-do of all three edits.
M002/S02's proof used `git checkout` safely only because the mutated file had
already been committed by its task 1.

`theory-afrobeat.mdx` was verified untouched (`git diff --quiet`) and its
Sources `See also refs [14]–[17]` listing intact. The companion-page rule
M002/S02 established still applies; applied here it correctly found nothing to
change.
