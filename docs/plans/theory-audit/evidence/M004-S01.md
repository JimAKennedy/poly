## M004/S01 — task 1: lay the dance beat and make the timeline legible

- `format` → exit 0
- `site-unit` → exit 0, 211 tests, 0 failures
- `doc-conformance` → exit 0, 190 tests, 0 failures
- 2026-09-10

The red came from removing S05's two `patch-divergence-ok` markers, which were
the only reason `ssa-dance-beat` and `ssa-timeline-legible` passed. Both then
failed on their own terms — `no lane at 12 steps / 4 hits`, and the Timeline
column absent from the header list — before anything was added.

The dance beat is lane 2, not an append: `theory-sub-saharan-africa` lays bell,
then dance beat, then supports, then lead, and a table that reads in
construction order is the thing the reader is meant to follow. 12 steps, 4 hits
— E(4,12), every third pulse — note 41 (GM Low Floor Tom, lower than the
support drum's 43 and otherwise unused here), velocity 95, between the bell's
110 and the support drum's 90, which is what "moderate" means against this
table rather than in the abstract.

The `Timeline` column reports the preset's own flags rather than asserting
them: `on` for the bell, because `Sub-Saharan: Agbekor` carries
`timeline: true` on lane 1, and `off` elsewhere. Rule 1 was already satisfied
in behaviour; what was missing was any way for a reader to see it. The column
sits after `Subdivision`, where `PresetTable` emits its conditional columns, so
a later migration off the hand-written table is a drop-in.

Both rules were watched to fail again after the fix, by inverse edit rather
than `git checkout --`, which would have destroyed the uncommitted work in this
file: removing the dance-beat lane fails `ssa-dance-beat`, removing the
`Timeline` header fails `ssa-timeline-legible`.

The suite's printed suppression count drops from 6 to 4. No marker names F37
any more.

## M004/S01 — task 2: slice close

- `format` → exit 0
- `site-unit` → exit 0, 211 tests, 0 failures
- `doc-conformance` → exit 0, 190 tests, 0 failures
- `e2e` → exit 0, `scripts/site-verify-local.sh`, all 13 stages: local audio,
  preset consistency, equivalence (27 chapters, 27 pass), sample equivalence
  (27 chapters), macro-diff (4 cases), first-bar parity (3 cases), lane mute,
  control audit, and the console-error gate (46 pages walked)
- `ledger` → exit 0, conforms with the slice `done`: both definition-of-done
  boxes ticked, this evidence file present, F37 closed
- 2026-09-10

`e2e` matters more than usual for this slice. Adding a lane and a column to a
chapter patch table changes what the Play card and the Try It modal render, and
the equivalence and sample-equivalence gates walk all 27 chapters comparing the
two. A table edit that broke that parity would not have shown up in
`site-unit`.

Marker inventory at close: four live, none naming F37.

| Rule | Page | Owning row |
|---|---|---|
| `ac-clave-approximation` | `03-afro-cuban.mdx` | F38 |
| `gam-pokok-layer` | `05-gamelan.mdx` | F41 |
| `gam-structural-overlap` | `05-gamelan.mdx` | F41 |
| `ebb-kick-avoids-snare` | `theory-electronic-breakbeat.mdx` | B15 |

No commit SHA is recorded here: this file ships inside the commit it describes.
