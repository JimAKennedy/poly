## M007/S03 — the marker burn-down

- `format` → exit 0
- `site-unit` → exit 0, 268 tests, 0 failures
- `doc-conformance` → exit 0
- `gate` → exit 0, all seven stages: build config, clang-format, RT safety,
  CodeSnippet region markers, build + test (589 ctest tests, 100% passed),
  pluginval at strictness 5, doc-conformance + audit-ledger guardrail
- `ledger` → exit 0, conforms with the slice `done`
- 2026-09-12

**There was nothing left to burn.** `grep -rn patch-divergence-ok site/` returns
nothing: five of M004/S05's six markers were removed by the slices that owed
them, and the sixth went with B15 in M007/S02. The count the suite prints is
zero.

So this slice is the guard rather than the clean-up, and the plan said so in
advance: the case passes on the day it is written, and its non-vacuity rests on
the deletion test rather than on that pass. Adding a marker reading "found by
the checklist, not yet triaged" to `theory-minimalism.mdx` fails it with
`theory-minimalism.mdx (min-deterministic)`, and it was removed by inverse edit.

**It forbids the placeholder, not the mechanism.** A marker carrying a reason
someone has accepted is legitimate and stays legitimate; what B14 rules out is a
suppression nobody has read since it was written. The failure message says to
resolve the ledger row that owns the marker rather than delete the marker,
because deleting it removes the record of a finding rather than the finding.

The live-count line M004/S05 built is retained and still prints. Zero today is
worth printing precisely because it is the number a later reader will compare
against.
