## M003/S02 — task 1: scope Chapter 6 to Hindustani and state the Carnatic absence

- `site-unit` → exit 0, 187 tests, 0 failures (2 new cases)
- `doc-conformance` → exit 0, 166 tests, 0 failures
- `format` → exit 0
- `ledger` → exit 0, conforms with the slice `done`: both definition-of-done
  boxes ticked, this file on disk, and F25 closed
- 2026-09-09

The chapter contained no Carnatic material to relabel. `Carnatic` appeared
exactly once in the file — in the description making the promise — so this
removed a false promise and then stated, positively, what is missing and where
to start on it.

Both cases failed on the forbidden side first, as the plan required.

**The requiring arm is what makes the case worth having.** `S02-F25` forbids
"Hindustani and Carnatic" *and* requires `Carnatic`, `solkattu` and `konnakol`
to be named. A forbid-only case would have gone green on deleting the word from
the description, satisfying the check while leaving the second
definition-of-done item — that the absence is stated rather than implied —
unmet. All three terms appeared zero times in the chapter beforehand, so each
was a real lock rather than a standing guard.

The overclaim's third site, the deep-dive index row in
`theory-counterpoint-overview.mdx`, was corrected in the same task by decision:
same claim, one table cell, and it advertised Carnatic rhythm one click from the
page that now says it is absent. The two cases were proved independent —
restoring the index row fails `S02-F25-overview` while `S02-F25` still passes,
so this is two guards, not one counted twice. Reverted with an inverse edit.

Four locks from earlier milestones live in this chapter and were asserted by
name rather than inferred from a green suite: `S02-F01`, `S02-F12`,
`S06-F16-indian` and `S04-F20` all passed.

The Scope note is scoped to the chapter, not the guide. M004/S04 will add a
tihai worked example from Nelson's *Solkattu Manual* — a Carnatic source — to
`theory-indian-classical.mdx`, and the appendix already lists it under Further
Reading, so a note claiming the guide holds no Carnatic material anywhere would
have been wrong on landing. The note points a reader at that entry instead.
