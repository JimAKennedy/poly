## M002/S01 — task 1: resolve `ref-2` against the publisher, correct it, and lock it

- `format` → exit 0
- `site-unit` → exit 0, 174 tests, 0 failures (2 new in `citation-tier.test.mjs`)
- `doc-conformance` → exit 0, 151 tests, 0 failures
- 2026-09-01

The finding was re-verified against the publisher before the correction, as the
plan's step 1 requires, and it moved the diagnosis rather than confirming it.
The audit's stated reason does not hold: MTO's issue index shows Volume 32
Number 2 (June 2026) current and Volume 31 complete through 2025, so 31(2) is
not a forward reference, and `mto.25.31.2.goldberg.pdf` resolves. What is wrong
is the title. The issue's table of contents lists seven articles; Goldberg's is
"Music Theory as an Instrument of Nationalism: Notation, Identity, and
Systemization in Dobri Hristov's Conception of Bulgarian Meter", and no issue
carries the printed title.

The gate bites, proved rather than assumed:

- before the correction, `S01-F17` failed on the forbidden side —
  `forbidden phrase reappeared: "Resultant Patterns in Phase-Shifted Rhythmic
  Structures"` — which is the failure the plan required to see first. A
  `present`-side failure would have meant the claim was mis-written.
- with the fabricated title appended to `01-foundations.mdx`, `S01-F17`
  **passed** and only `S01-F17-tree` failed, naming `01-foundations.mdx`. That
  is the whole reason the tree-wide case exists: the per-file case structurally
  cannot see a file it does not read. Reverted and confirmed with
  `git diff --quiet`.

`ref-2` remains cited by nothing — no `.mdx` under the docs root contains
`#ref-2`. That is deliberate and stays true after this slice: M002/S05 owns the
Chapter 7 citations that would draw on it, and depends on M001/S04.
