## M005/S03 — task 1: Powers added, Kippen locked

- `format` → exit 0
- `site-unit` → exit 0, 221 tests, 0 failures
- `doc-conformance` → exit 0
- `npm --prefix site run build` → exit 0
- `ledger` → exit 0, conforms with the slice `done`
- 2026-09-10

**Half of F48 was already done and the run confirmed it rather than assuming
it.** The row says Kippen (1988) is "in Further Reading only". He is not:
`06-indian-classical.mdx` cites him inline at the theka claim — "learns by rote,
not Bjorklund distributions" — added by M002/S04. The `M005 F48-kippen` case
passed on the day it was written, which is why its non-vacuity rests on the
deletion test below rather than on that pass.

**Powers is cited for what verification supports and no more.** The *New Grove*
"India, subcontinent of" article exists and is his; that it covers the Carnatic
tala system specifically was not established. So he is cited at the chapter's
scope statement — where it says Carnatic practice is "not covered here" and
points at Nelson — as the survey of the subcontinent's art-music theory that
this chapter narrows from. Attaching him to a Carnatic tala claim would have
asserted exactly what could not be checked.

Both arms proved to bite, each restored by inverse edit:

- Powers' citation removed → `fr-powers-1980 is not cited near "None of that is
  covered here"`
- Kippen's existing citation removed → `fr-kippen-1988 is not cited near "learns
  by rote"`

No commit SHA is recorded here: this file ships inside the commit it describes.
