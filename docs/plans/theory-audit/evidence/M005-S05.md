## M005/S05 — task 1: Scherzinger in the chapter that makes the claim

- `format` → exit 0
- `site-unit` → exit 0, 225 tests, 0 failures
- `doc-conformance` → exit 0
- 2026-09-11

F50 needed no appendix entry: `fr-scherzinger-2010` already existed and was
already cited from `theory-minimalism.mdx`. What was missing was a citation from
Chapter 8, which is where the claim about the African–minimalist connection
actually gets made — the chapter had zero mentions of him.

He now sits beside the Agawu (2003) citation M005/S01 added at the same reframe,
and the two do different jobs: Agawu raises the prior question of who describes
African music and on whose terms, Scherzinger takes up the comparison itself and
is more critical of it than Reich was. Placing one in front of the other rather
than replacing was the plan's instruction, and it is right — they are not
substitutes.

Watched to fail before the citation and again with it removed —
`fr-scherzinger-2010 is not cited near "West African and Indonesian musicians
already knew"` — restored by inverse edit.

**Execution correction.** F51's two cases were written in this task by mistake;
the plan puts them in task 2. Left in place they would have made this task
commit with a red suite, which `/jk:next` forbids. They were moved out and will
be written in task 2, where the entry they assert is created.

## M005/S05 — task 2: Born & Hesmondhalgh at the cross-pollination argument

- `format` → exit 0
- `site-unit` → exit 0, 226 tests, 0 failures
- `doc-conformance` → exit 0, 205 tests, 0 failures
- `npm --prefix site run build` → exit 0
- `ledger` → exit 0, conforms with the slice `done`
- 2026-09-11

**They are the volume's editors, not its authors**, and the entry says `eds.`
F51's `Item` records why that mattered enough to check.

The appendix had no Further Reading subsection for Chapter 14 — only a numbered
`## Chapter 14: Synthesis` section carrying `ref-44`. A `### Synthesis (Chapter
14)` subsection was added, because this is a framing work rather than the source
of a specific claim, and the guide's own grammar puts framing works in Further
Reading with `fr-` anchors and inline author-year links.

The citation opens "Cross-Pollination: Why It Works" rather than closing it: the
section argues that traditions combine well when their step counts share a low
common multiple, and the volume's subject is when musical borrowing works and on
whose terms. Saying that before the technical argument rather than after is the
honest order — the collection is not evidence for the Euclidean point, it is the
frame the point sits inside.

Both arms proved to bite separately, each restored by inverse edit:

- entry removed → `fr-born-hesmondhalgh-2000 (ledger row F51)`
- citation removed → `fr-born-hesmondhalgh-2000 is not cited near
  "Cross-Pollination: Why It Works"`

No commit SHA is recorded here: this file ships inside the commit it describes.
