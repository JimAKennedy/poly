## M005/S01 — task 1: the sub-Saharan entries

- `format` → exit 0
- `site-unit` → exit 0, 215 tests, 0 failures
- `doc-conformance` → exit 0, 194 tests, 0 failures
- 2026-09-10

`site/tests/literature-enrichment.test.mjs` is M005's host, created here and
wired into both `scripts/check-doc-conformance.sh` and the `REQUIRED` array of
`doc-conformance-wiring.test.mjs` — following `scope-framing.test.mjs`, M003's
host. Both are needed: nothing in CI runs `npm --prefix site test` (poly issue
#272), so a host outside the runner never runs remotely, and `REQUIRED` is what
stops a later edit quietly dropping it from the runner.

Three entries, all tier A, all verified before planning:

- Charry, E. (2000). *Mande Music*. University of Chicago Press.
- Kubik, G. (1999). *Africa and the Blues*. University Press of Mississippi.
- Agawu, K. (2003). *Representing African Music*. Routledge.

Agawu (2003) sits directly above the existing `fr-agawu-2006`, and its
annotation says how they differ — a critique of how African music is written
about, against a narrower methodological argument about structural versus
cultural analysis. Without that, the pair reads like one entry duplicated with a
typo in the year.

The case names the ledger row alongside each missing anchor, so a failure says
which finding is unmet rather than only which string is absent. Watched to fail
with all three missing, and again with Kubik alone removed — `fr-kubik-1999
(ledger row F45)` — restored by inverse edit.

The entries are listed but not yet cited. That is deliberate and is why this
task closes no row: an entry nobody cites is precisely what F44, F45 and F46
complain about, so the rows close in task 2.
