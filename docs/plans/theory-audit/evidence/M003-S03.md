## M003/S03 — task 1: Chapter 2, mark the Manding same-cycle flattening

- `format` → exit 0
- `site-unit` → exit 0, 188 tests, 0 failures (1 new case, `S03-F26`)
- `doc-conformance` → exit 0, 167 tests, 0 failures
- 2026-09-09

`S03-F26` failed on the **present** side first — `corrective phrase went
missing: "pedagogical simplification"`. It carries no forbidden arm, because the
original sentence is not wrong: Manding patterns often do share a cycle length.
What was missing was the admission that this is not the whole tradition.

The disclosure gives the reason for the simplification rather than only
confessing it — the shared cycle is the shortest route to a playable patch that
sounds idiomatic — and says what varies: the dunun, sangban and kenkeni
relationship differs by repertoire and region.

**Charry (2000) is named with no anchor, and that is deliberate.** Verified:
zero `fr-charry` or `#charry` links in the file. The appendix entry belongs to
F44 in M005/S01, whose own verification reads "cited inline at the F26
disclosure" and which depends on this slice; linking now would point at an entry
that does not exist and fail `research-provenance`, which was run explicitly and
reports `all citations resolve`. Adding the entry here instead would leave F44
with nothing to close.

M001's two locks on this chapter, `S05-F07` and `S05-F08`, were confirmed by
name rather than inferred from a green suite.

## M003/S03 — task 2: gamelan Rules 3, 4 and 5

- `format` → exit 0
- `site-unit` → exit 0, 191 tests, 0 failures (3 new cases)
- `doc-conformance` → exit 0, 170 tests, 0 failures
- 2026-09-09

Two prose edits, three rows. Rule 3 gained the style-dependence caveat naming
*norot* as the reversing case; Rule 5's absolute prohibition became a stated
discipline rather than a rule of the tradition, citing Tenzer for the mixing he
documents within a single kebyar performance. Rule 4 was not edited at all.

`S03-F27` failed on the present side and `S03-F30` on the forbidden side, each
as the plan predicted. **`S03-F29` passed the moment it was written**, because
Rule 4's honesty was already there — that is what its `accept` disposition
means, and the plan said to expect it rather than treat it as a mistake.

**The accept row's case was proved to bite.** Deleting "Strict complementation
is only the textbook case." from Rule 4 fails `S03-F29` naming that exact phrase.
Restored with an inverse edit, and `git diff --stat` confirmed the file was back
to the two lines this task intended to change — a case written against text that
already exists is worthless until it has been seen to fail.

**Two assertion arms were deliberately not used**, and the reason is worth
keeping: `norot` and `Tenzer` both already appear in this file, in Rules 5 and 4
respectively, so asserting either would have passed before any edit and proved
nothing about the rules being changed. The cases assert `style-dependent`,
`in norot the relationship is effectively reversed`, `reliable default` and
`stylistic mixing`, all four of which were absent beforehand.

**A plan inaccuracy, corrected here rather than propagated.** The plan's context
note said `theory-gamelan.mdx` carries `S06-F16-gamelan`. It does not: that case
guards `05-gamelan.mdx`, the chapter. The lock that actually guards this file is
`theory-gamelan: polos has 5 hits and four colotomic strata are present` in
`theory-patch-conformance.test.mjs`, and it passes. The instruction — run
`site-unit` immediately and confirm the file's locks — was followed and is
satisfied; only the case name in the plan was wrong.

## M003/S03 — task 3: Chapter 6, layakari is not a subdivision change

- `site-unit` → exit 0, 192 tests, 0 failures (1 new case, `S03-F28`)
- `doc-conformance` → exit 0, 171 tests, 0 failures
- `format` → exit 0
- `ledger` → exit 0, conforms with the slice `done`: all five
  definition-of-done boxes ticked, this file on disk, and F26 through F30 closed
- 2026-09-09

`S03-F28` failed on the present side — `corrective phrase went missing: "hit
density"`. No forbidden arm: the subdivision mapping is a legitimate Poly
workflow and stays. What was missing was the distinction between it and what
layakari actually does.

The note keeps the recipe and adds the concrete difference rather than a general
hedge: layakari moves the rate of unchanged material, a subdivision change
alters how many onsets fall in the cycle, and a tabla player performing
*chaugun* is not adding hits. It is placed after the Poly workflow paragraph so
a reader meets the recipe first and then what it approximates.

**Finding the paragraph by text rather than line number was necessary, not
cautious.** The plan warned that M003/S02's Scope note had moved it; the
paragraph is at line 64, where older notes record 54. A line-addressed edit
would have landed ten lines high, inside the tala discussion.

Six locks from earlier milestones live in this chapter and all passed:
`S02-F01`, `S02-F12`, `S06-F16-indian`, `S04-F20`, `S02-F25` — the five the plan
named — and `S03-F5` from `idiom-break-framing.test.mjs`, which it did not. The
plan's list was short by one; the instruction to run `site-unit` immediately
covered it anyway, which is the argument for running the whole suite rather than
only the cases a plan enumerates.
