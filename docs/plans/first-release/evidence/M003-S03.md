## M003/S03 — task 1: the guard, and the slice closes

- `format` → exit 0
- `site-unit` → exit 0, **332 pass, 0 fail**
- `doc-conformance` → exit 0
- `ledger` → exit 0, 5 ledgers conform
- 2026-09-22

`site/tests/appendix-entry-rules.test.mjs` asserts three properties of the
shipping appendix and never a count: **cited** (every entry is cited from a
shipping page other than the appendix), **tier** (every entry is A or B), and
**obtainable** (no entry's manifest record is `library-only`). It reads the
shipping `.mdx` files and the manifest, and does not read the theory bundle.

**All three arms passed on the tree M003/S02 left**, which proves nothing, so
each was made to fail. Every mutation was reverted and the file confirmed
byte-identical by MD5 before the next:

| mutation | arm | result |
|---|---|---|
| an entry no page cites is added to the appendix | cited | **red** — `fr-mutation-2026` named; the obtainable arm also red, `(no manifest record)` |
| `fr-vitale-1990` re-tiered to `C` | tier | **red** — `fr-vitale-1990 (C)` named |
| `fr-cohn-1992` set to `library-only` in the manifest | obtainable | **red** — `fr-cohn-1992 (library-only)` named |
| every entry removed from the appendix | all three | **red** — each arm **throws** `defines no entries` rather than passing vacuously |

The fourth is the one that matters most: it is the failure mode two earlier
guards in this repo have hit, and the reason `presets-json-schema.test.mjs`
carries the note it does.

### Two amendments, both the owner's, both recorded

**The tier arm allows B.** The DoD says "not Tier A". `fr-linn-attack-2020`
is Tier B — Roger Linn describing the MPC's timing in his own words, cited in
preference to commentary quoting him second-hand — and B is the honest label
for an interview. C is what marks unusable material, and the guard fails on C.

**One phrase exempts an entry from the cited and obtainable arms, and only
those.** `fr-peycheva-dimov-2002` says "contents unverified" and that no claim
rests on it; M005/S04 pins the phrase. The FR19 decision kept it on that
declaration, and the 2026-09-22 decision applied the same declaration to
obtainability, so the guard exempts it in both arms as one rule rather than
two. The tier arm is not exempt: an unread source still has a venue.

### The appendix at close

| | |
|---|---|
| entries | **41** |
| cited from a shipping page | 40 |
| exempt by declaration | 1 |
| Tier A / B / C | 39 / 2 / 0 |
| `library-only` among the 40 cited | 0 |

FR20 closes. FR23 was already `accepted`. Every slice of M003 is `done`; the
milestone is ready to ship.
