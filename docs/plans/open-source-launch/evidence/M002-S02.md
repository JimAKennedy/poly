## M002/S02 — task 1: both targets and the seeder build under a declared option, and each runs clean on its seeds

- `fuzz` → exit 0: `fuzz_seed_corpus` wrote 10 seed files; `fuzz_state_io`
  705,188 runs in 61 s, cov 639, corpus 5 → 778; `fuzz_midi_reader`
  569,519 runs in 61 s, cov 329, corpus 5 → 190; no crash, no timeout
- `engine-isolation` → exit 0, 569 of 569
- `sanitizers` → exit 0 — engine-only ASan, UBSan, TSan 569 of 569 each;
  plugin ASan 61 host and 59 bridge; plugin TSan 61 host
- `format` → exit 0
- 2026-09-24

**The option exists.** `BUILD_FUZZ_TESTS` is declared in the top-level
`CMakeLists.txt` beside the sanitizer options. Configuring with it `ON` under
Homebrew LLVM Clang 22 built `fuzz_state_io` — the first time the target has
been built on any machine since `tests/CMakeLists.txt` gated it on an option
nothing declared.

**Three targets, one option.** `fuzz_midi_reader` runs the dropped bytes
through `parseSMF`, the fitter, and `importMidiToLane` on a kick lane;
`fuzz_seed_corpus <dir>` writes the default and three factory-preset groove
states, the default scene state, three engine-written Euclidean SMFs, a
two-instrument SMF, and a Format-1 multitrack SMF. Generated at build time,
never committed.

**The engine is instrumented.** The first seeded run reached 40 edges in the
MIDI reader after 780,780 executions and 247 in state I/O: `-fsanitize=fuzzer`
instruments only the target's own translation unit. With
`-fsanitize=fuzzer-no-link` on the whole fuzz build (option-gated), the same
60 seconds reach 329 and 639. Recorded as a judgment call in
`M002-decisions.md`.

No row closes yet: OS09 and OS10 close on task 3's planted-bug proof.

## M002/S02 — task 2: the nightly builds, seeds and runs them, and a crash files an issue

- `node --test scripts/check-workflow-hygiene.mjs` → exit 0, 22 of 22
- `jk-standards action-pinning`, `workflow-permissions` → pass
- `sanitizers.yml` parses; jobs: asan, ubsan, tsan, asan-plugin,
  tsan-plugin, fuzz, notify; `notify.needs` lists all six
- `format` → exit 0
- 2026-09-24

**The job.** `fuzz` configures with Clang, ASan and `BUILD_FUZZ_TESTS=ON`,
builds the three targets, seeds with `fuzz_seed_corpus corpus`, and runs each
fuzzer for 300 seconds with a 10-second per-input timeout and a 2 GB RSS
limit, writing reproducers under `artifacts/`. On failure the reproducers are
uploaded as `fuzz-artifacts`. `notify` now depends on `fuzz` and appends
`FUZZ` to its failure list, so a crash comments on or opens the
`sanitizer-failure` issue exactly as a sanitizer failure does. The top-level
`contents: read` from M002/S01 covers it.

No row closes yet.

## M002/S02 — task 3: each target finds a planted bug, the option is documented, and the slice closes

- `fuzz` → exit 0 on the restored tree (60 s per target, see below)
- `engine-isolation` → exit 0, 569 of 569
- `sanitizers` → exit 0, all five variants
- `format` → exit 0
- `doc-discipline` → exit 0; the `tests/CMakeLists.txt` ↔
  `docs/testing-strategy.md` pairing is satisfied by the doc edit, and the
  doc's status anchor moved to today
- `ledger` → exit 0, 6 ledgers conform
- 2026-09-24

**Both targets found their planted bug in under a second.** On a scratch
branch `scratch/M002-S02-planted`, never pushed and deleted afterwards:

| Target | Planted | Found after | ASan report |
|---|---|---|---|
| `fuzz_state_io` | `cellSizes[cellCount] = 0` with `cellCount` read unvalidated from the file, in `readLaneConfig` | 1 s | `AddressSanitizer: BUS` at `state_io_read_lane.h:126` in `readLaneConfig`; reproducer written |
| `fuzz_midi_reader` | the `trackEnd - c.p < dataBytes` check before `d1 = c.u8()` removed | under 1 s | `AddressSanitizer: heap-buffer-overflow` at `midi_reader.cpp:23` in `Cursor::u8()`, called from `parseSMF` at the planted line; reproducer written |

The state-I/O plant differs from the plan's step; the plan's one-past read
stays inside the object, and the decisions file records why the substitute
was made.

**Documented.** `CONTRIBUTING.md` gains a Fuzzing subsection: the option,
the Clang requirement, the seeder, the two targets, the nightly, and that a
finding is a security report. `docs/testing-strategy.md`'s L1 inventory names
both targets and the seeder.

### The slice at close

| DoD | Satisfied by |
|---|---|
| The fuzz option is declared, documented, and built by a workflow | task 1 declares `BUILD_FUZZ_TESTS`; task 3 documents it; task 2's nightly builds it |
| Both untrusted inputs have a fuzz target with a seed corpus | task 1 — `fuzz_state_io` and `fuzz_midi_reader`, seeded by `fuzz_seed_corpus` |
| The nightly runs each for a bounded time and files an issue on a crash | task 2 — 300 s per target; `notify` extended |
| Each target is shown to find a bug planted on a scratch branch | task 3 — the table above |

OS09 and OS10 close. The slice is `done`.
