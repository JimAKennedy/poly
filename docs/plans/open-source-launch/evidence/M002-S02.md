## M002/S02 — task 1: both targets and the seeder build under a declared option, and each runs clean on its seeds

- `fuzz` → exit 0: `fuzz_seed_corpus` wrote 10 seed files; `fuzz_state_io`
  705,188 runs in 61 s, cov 639, corpus 5 → 778; `fuzz_midi_reader`
  569,519 runs in 61 s, cov 329, corpus 5 → 190; no crash, no timeout
- `engine-isolation` → exit 0, 569 of 569
- `sanitizers` → exit 0 — engine-only ASan, UBSan, TSan 569 of 569 each;
  plugin ASan 61 host and 59 bridge; plugin TSan 61 host
- `format` → exit 0
- 2026-09-24

**The option exists.** `BUILD_FUZZ_TESTS` is declared in the top-level
`CMakeLists.txt` beside the sanitizer options. Configuring with it `ON` under
Homebrew LLVM Clang 22 built `fuzz_state_io` — the first time the target has
been built on any machine since `tests/CMakeLists.txt` gated it on an option
nothing declared.

**Three targets, one option.** `fuzz_midi_reader` runs the dropped bytes
through `parseSMF`, the fitter, and `importMidiToLane` on a kick lane;
`fuzz_seed_corpus <dir>` writes the default and three factory-preset groove
states, the default scene state, three engine-written Euclidean SMFs, a
two-instrument SMF, and a Format-1 multitrack SMF. Generated at build time,
never committed.

**The engine is instrumented.** The first seeded run reached 40 edges in the
MIDI reader after 780,780 executions and 247 in state I/O: `-fsanitize=fuzzer`
instruments only the target's own translation unit. With
`-fsanitize=fuzzer-no-link` on the whole fuzz build (option-gated), the same
60 seconds reach 329 and 639. Recorded as a judgment call in
`M002-decisions.md`.

No row closes yet: OS09 and OS10 close on task 3's planted-bug proof.
