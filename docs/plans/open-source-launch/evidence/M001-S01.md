## M001/S01 — task 1: the plugin reports the CMake version, and a guard keeps it that way

- `format` → exit 0
- `unit` → exit 0, **698 of 698 tests passed**
- `guards` → exit 0 (62 contract cases, including the new `version-source`)
- `site-unit` → exit 0, 331 pass; `webui-e2e` → exit 0, 360 passed — the
  versionless private manifests load
- 2026-09-23

**The version is typed once.** `project(poly VERSION 0.2.0)`;
`smtg_target_configure_version_file(poly_plugin)` generates
`projectversion.h`; `plugids.h` reads `FULL_VERSION_STR` from it. Both npm
manifests lost their `version` and are `"private": true`.

**Proved by reading the bundle, three times.** After each build,
`build/VST3/Release/poly_plugin.vst3/Contents/Resources/moduleinfo.json` was
read for its module version and both class versions:

| `project(poly VERSION …)` | module | processor class | controller class |
|---|---|---|---|
| before this task | `0.1.0` | `1.0.0` | `1.0.0` |
| `0.2.0` | `0.2.0` | `0.2.0` | `0.2.0` |
| `0.2.1` (mutation) | `0.2.1` | `0.2.1` | `0.2.1` |
| `0.2.0` (restored) | `0.2.0` | `0.2.0` | `0.2.0` |

**The guard was seen red first.** `scripts/check-version-source.mjs` failed
three of four cases on the unmodified tree — the literal in `plugids.h`, and
a `version` in each manifest — and passes four of four now. The fourth case,
that `CMakeLists.txt` declares a project version, guards a removal and could
not fail on either tree.

### Two things the plan got wrong, corrected before they ran

**The SDK helper must be called from the top-level list file.** Called from
`plugin/CMakeLists.txt` it wrote `projectversion.h` into `build/plugin/` while
adding `build/` to the include path, and the first build failed with
`'projectversion.h' file not found`. `configure_file` resolves against the
*current* binary directory and the helper adds `PROJECT_BINARY_DIR`; the two
coincide only at the top level. The call now sits beside
`add_subdirectory(plugin)` with the reason in a comment.

**Three test targets compile plugin sources directly.** `poly_host_tests`,
`poly_probe_tests` and `poly_bridge_tests` include `plugin/source` rather than
linking `poly_plugin`, so the include directory the helper adds to the plugin
never reached them and the full build failed the same way. Each gains
`${PROJECT_BINARY_DIR}` beside `plugin/source`, with a comment.

OS01 closes.

## M001/S01 — task 2: the changelog has the section the first tag will publish, and the slice closes

- `format` → exit 0
- `unit` → exit 0, 698 of 698
- `guards` → exit 0
- `doc-discipline` → exit 0 once this commit's `Docs-Not-Affected` trailers
  cover task 1's range (see below); the other arms passed throughout
- `ledger` → exit 0, 6 ledgers conform
- 2026-09-23

**`## [Unreleased]` is now `## [0.2.0] - unreleased`.** Nothing inside the
section changed. `node scripts/gen-release-notes.mjs 0.2.0` prints it — 13,296
words, which is M005's problem, not this slice's — and `Unreleased` now fails
loud, since no such section exists until M006 dates this one and opens a new
one.

**The contract test ties the changelog to the build.** The case that proved
the generator on `Unreleased` now reads `project(poly VERSION …)` from
`CMakeLists.txt` and asks the generator for that version. Seen red before the
rename — 26 pass, 1 fail — and green after — 27 pass. A version bump without a
changelog section now fails `guards` on every run instead of at tag time.

**Doc drift, and why the trailers are honest.** The drift map pairs
`tests/CMakeLists.txt` and `site/package.json` with `docs/testing-strategy.md`,
and `plugin/source/plugids.h` with `docs/cubase-workflow.md`. Task 1 touched
all three. The tests change adds one include directory to three existing
targets, no new binary and no taxonomy change; the manifest change removes a
version nobody read; `plugids.h` changes where a string comes from, not what a
Cubase user sees. Each is declared with its reason in this commit's trailers.

### The slice at close

| DoD | Satisfied by |
|---|---|
| `project(poly VERSION …)` is the only hand-edited version that reaches a shipped artifact | task 1 — `plugids.h` reads the generated header; manifests are versionless; the AU plist is OS17's |
| The factory class info reports that version, proved by change and read-back | task 1 — 0.2.0 → 0.2.1 → 0.2.0 in `moduleinfo.json` |
| A check fails if a hand-typed version returns, seen red first | task 1 — `check-version-source.mjs`, three of four red on the old tree |
| The first version is decided and recorded, and the changelog section for it describes the current tree | task 2 — 0.2.0 in `M001-decisions.md`; `## [0.2.0] - unreleased` holds every entry since June |

OS02 closes; OS01 closed with task 1. The slice is `done`.
