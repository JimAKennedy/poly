## M001/S02 — task 1: the release runs the tests on what it packages

- `format` → exit 0
- `guards` → exit 0 (63 contract cases; the new one included)
- 2026-09-23

**A `Run tests` step now sits between Build and pluginval on both legs**,
running `ctest --test-dir build --build-config Release --output-on-failure`
unconditionally. The release configuration already built the test targets —
`add_subdirectory(tests)` is unconditional — so the step adds no build, only
the run that was missing. On the macOS leg that is the first time the
universal binary's tests execute anywhere.

**The contract test was seen red, green, and red again:**

| tree | result |
|---|---|
| before the step | **red** — `missing "Run tests" step` (27 pass, 1 fail) |
| with the step | green (28 pass) |
| step removed | **red** — same message; restored, 28 pass |

The test also asserts the step comes after Build and before both pluginval
steps, invokes ctest with `--build-config Release` (the Visual Studio
generator needs it), and carries no `if:`, so a later edit cannot quietly make
it macOS-only.

OS03 closes.

## M001/S02 — task 2: the release publishes checksums and provenance

- `format` → exit 0
- `guards` → exit 0 (66 contract cases; three new)
- `action-pinning` → all action references SHA-pinned, the new one included
- 2026-09-23

**The release job now writes `SHA256SUMS` over every zip and attests build
provenance for each**, both between downloading the artifacts and generating
the notes, and `SHA256SUMS` ships beside the zips. The job's permissions are
exactly `contents`, `id-token` and `attestations: write`; the new test
asserts that set and nothing else, so a later grant has to be explained.

**Seen red, green, and red again:**

| tree | result |
|---|---|
| before the edit | **red** — 3 fail (grants, checksums, attestation) |
| with the edit | green (31 pass) |
| attestation step removed | **red** — `release job must use actions/attest-build-provenance@4d1014…`; restored, 31 pass |

The action is `actions/attest-build-provenance` v4.2.2 at
`4d101475d8b20a2381f78447822ac1eab6504dd8`, resolved from the tag on
2026-09-23 and pinned like every other action in the tree. A downloader
verifies with `gh attestation verify <zip> --owner JimAKennedy`; the comment
beside the step says so.

OS04 closes.

## M001/S02 — task 3: every pluginval download is verified, and the slice closes

- `format` → exit 0
- `guards` → exit 0 (67 contract cases; one new)
- `doc-discipline` → exit 0
- `ledger` → exit 0, 6 ledgers conform
- 2026-09-23

**All four download sites verify the bytes before unzipping them** —
`ci.yml`'s pluginval-macos and pluginval-windows jobs, and both legs of
`release.yml` — with `echo "<digest>  pluginval.zip" | shasum -a 256 -c`. All
four steps run under bash (the Windows ones by job default or step `shell:`),
so one command serves every platform. The digests are the v1.0.4 release
assets' SHA-256, computed from two fresh downloads on 2026-09-23:

| asset | SHA-256 |
|---|---|
| `pluginval_macOS.zip` | `3c4c533bda0c5059eea3ddaea752d757ee2025041f0f47e6bcb0e87f6082b29f` |
| `pluginval_Windows.zip` | `c08e61ce3b96db41636f8ec7e76f4c7e2c13ebdac7fa1b5a1f52b4f32ec715ab` |

**The contract test reads both workflows**, requires exactly four sites so a
fifth unverified download cannot slip in unnoticed, and checks each digest
against the constant pinned in the test. Seen red at all four sites before
the edit, green after, and red again when one digit of `ci.yml`'s Windows
digest was changed — naming the file, the asset and the mismatch.

### The slice at close

| DoD | Satisfied by |
|---|---|
| The release build runs the unit and golden tests on the configuration it packages, before packaging | task 1 — `Run tests` between Build and pluginval on both legs |
| Every Release carries `SHA256SUMS` and a build-provenance attestation per zip | task 2 — `Write checksums` and `Attest build provenance` before publish; `SHA256SUMS` in the Release files |
| Every pluginval download in every workflow is verified against a pinned digest | task 3 — four sites, `shasum -a 256 -c` |
| `check-release-workflow.mjs` asserts all three, each seen red | 27 → 32 cases; every addition red by removal or tampering before being trusted |

OS05 closes; OS03 and OS04 closed with their tasks. The slice is `done`, and
with it every slice of M001.
