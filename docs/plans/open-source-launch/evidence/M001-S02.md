## M001/S02 — task 1: the release runs the tests on what it packages

- `format` → exit 0
- `guards` → exit 0 (63 contract cases; the new one included)
- 2026-09-23

**A `Run tests` step now sits between Build and pluginval on both legs**,
running `ctest --test-dir build --build-config Release --output-on-failure`
unconditionally. The release configuration already built the test targets —
`add_subdirectory(tests)` is unconditional — so the step adds no build, only
the run that was missing. On the macOS leg that is the first time the
universal binary's tests execute anywhere.

**The contract test was seen red, green, and red again:**

| tree | result |
|---|---|
| before the step | **red** — `missing "Run tests" step` (27 pass, 1 fail) |
| with the step | green (28 pass) |
| step removed | **red** — same message; restored, 28 pass |

The test also asserts the step comes after Build and before both pluginval
steps, invokes ctest with `--build-config Release` (the Visual Studio
generator needs it), and carries no `if:`, so a later edit cannot quietly make
it macOS-only.

OS03 closes.

## M001/S02 — task 2: the release publishes checksums and provenance

- `format` → exit 0
- `guards` → exit 0 (66 contract cases; three new)
- `action-pinning` → all action references SHA-pinned, the new one included
- 2026-09-23

**The release job now writes `SHA256SUMS` over every zip and attests build
provenance for each**, both between downloading the artifacts and generating
the notes, and `SHA256SUMS` ships beside the zips. The job's permissions are
exactly `contents`, `id-token` and `attestations: write`; the new test
asserts that set and nothing else, so a later grant has to be explained.

**Seen red, green, and red again:**

| tree | result |
|---|---|
| before the edit | **red** — 3 fail (grants, checksums, attestation) |
| with the edit | green (31 pass) |
| attestation step removed | **red** — `release job must use actions/attest-build-provenance@4d1014…`; restored, 31 pass |

The action is `actions/attest-build-provenance` v4.2.2 at
`4d101475d8b20a2381f78447822ac1eab6504dd8`, resolved from the tag on
2026-09-23 and pinned like every other action in the tree. A downloader
verifies with `gh attestation verify <zip> --owner JimAKennedy`; the comment
beside the step says so.

OS04 closes.
