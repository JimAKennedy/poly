## M001/S01 — task 1: `ref-6` links to Jones itself

- `format` → exit 0, 0 failed hooks
- `site-unit` → exit 0, 307 tests, 307 pass, 0 fail
- `doc-conformance` → exit 0, 281 tests, 281 pass, 0 fail
- 2026-09-20

`REF6-JONES` was seen red before the edit, on the forbidden arm:
`forbidden pattern reappeared: /id="ref-6"(?:(?!archive\.org)[^\n])*cambridge\.org/`.

The case bites in three directions — each mutation applied to
`appendix-references.mdx`, tested, and reverted, with the file confirmed
byte-identical afterwards:

- revert to the old single-link form → red
- move the review ahead of the scan link → red
- point the scan link at a different archive.org item → red

The third is the one the present arm alone would miss: `presentRegex` only
proves the scan URL exists somewhere in the file, while the forbidden arm
proves it is on `ref-6`'s own line and ahead of the commentary.

The definition of done's ISBN arm was not taken because it is unavailable, not
because the scan arm was preferred: the archive.org record carries no ISBN
field and a 1959 Oxford University Press imprint predates the ISBN system.

## M001/S01 — task 2: `ref-26` becomes an open-access aksak paper

- `format` → exit 0, 0 failed hooks
- `site-unit` → exit 0, 308 tests, 308 pass, 0 fail
- `doc-conformance` → exit 0, 282 tests, 282 pass, 0 fail
- 2026-09-20

`REF26-AKSAK` was seen red before the edit, with both arms failing: `fiveable.me`
still present and the DOI absent.

Three mutations, each applied, tested and reverted:

- the `fiveable.me` URL returns → red
- the DOI is removed and the citation text kept → red
- the entry is swapped for the Goldberg paper from the same issue → red

The third guards a specific mistake rather than a generic one. `fr-goldberg-2015`
is already "Timing Variations in Two Balkan Percussion Performances" from this
same special issue, so reaching for the most famous article in it would have
duplicated a source the Balkan chapter already cites.

The DOI was resolved rather than assumed: `https://doi.org/10.18061/emr.v10i4.4891`
returns 200 and lands on `emusicology.org/article/id/4759/`, and Crossref records
that article at 10(4), 265–291 — the pages the entry claims.
