## M001/S01 — task 1: `ref-6` links to Jones itself

- `format` → exit 0, 0 failed hooks
- `site-unit` → exit 0, 307 tests, 307 pass, 0 fail
- `doc-conformance` → exit 0, 281 tests, 281 pass, 0 fail
- 2026-09-20

`REF6-JONES` was seen red before the edit, on the forbidden arm:
`forbidden pattern reappeared: /id="ref-6"(?:(?!archive\.org)[^\n])*cambridge\.org/`.

The case bites in three directions — each mutation applied to
`appendix-references.mdx`, tested, and reverted, with the file confirmed
byte-identical afterwards:

- revert to the old single-link form → red
- move the review ahead of the scan link → red
- point the scan link at a different archive.org item → red

The third is the one the present arm alone would miss: `presentRegex` only
proves the scan URL exists somewhere in the file, while the forbidden arm
proves it is on `ref-6`'s own line and ahead of the commentary.

The definition of done's ISBN arm was not taken because it is unavailable, not
because the scan arm was preferred: the archive.org record carries no ISBN
field and a 1959 Oxford University Press imprint predates the ISBN system.

## M001/S01 — task 2: `ref-26` becomes an open-access aksak paper

- `format` → exit 0, 0 failed hooks
- `site-unit` → exit 0, 308 tests, 308 pass, 0 fail
- `doc-conformance` → exit 0, 282 tests, 282 pass, 0 fail
- 2026-09-20

`REF26-AKSAK` was seen red before the edit, with both arms failing: `fiveable.me`
still present and the DOI absent.

Three mutations, each applied, tested and reverted:

- the `fiveable.me` URL returns → red
- the DOI is removed and the citation text kept → red
- the entry is swapped for the Goldberg paper from the same issue → red

The third guards a specific mistake rather than a generic one. `fr-goldberg-2015`
is already "Timing Variations in Two Balkan Percussion Performances" from this
same special issue, so reaching for the most famous article in it would have
duplicated a source the Balkan chapter already cites.

The DOI was resolved rather than assumed: `https://doi.org/10.18061/emr.v10i4.4891`
returns 200 and lands on `emusicology.org/article/id/4759/`, and Crossref records
that article at 10(4), 265–291 — the pages the entry claims.

## M001/S01 — task 3: `ref-22` is measured, recorded, and queued

- `format` → exit 0, 0 failed hooks
- `site-unit` → exit 0, 309 tests, 309 pass, 0 fail
- `doc-conformance` → exit 0, 283 tests, 283 pass, 0 fail
- 2026-09-20

The queue test was seen red before the worklist existed: *"appendix-references.mdx
still cites nios.ac.in, but …/browser-worklist.md does not exist."*

It is an implication, and was proved to behave as one across all three states
rather than only in the direction that happened to be red:

| bibliography cites `nios.ac.in` | worklist names `ref-22` | result |
|---|---|---|
| yes | no | **red** |
| no | yes | green |
| no | no | green |

The middle row is the one that matters. A claim pointed at the worklist alone
could only assert the obligation unconditionally, and would go red the day
`ref-22` is legitimately resolved — passing today for a reason that guarantees
a wrong failure later.

VR02 does **not** close here. What was measured is that nothing on `nios.ac.in`
answers from this network: the PDF times out at 60s, a sibling chapter a search
engine lists as live times out identically, the site root times out, and DNS
resolves to a single A record. What was not measured is whether the URL works,
which no run here can settle.

## M001/S01 — task 4: `ref-22` replaced on editorial grounds

- `format` → exit 0 (first run exit 1: `end-of-file-fixer` added a final newline
  to `browser-worklist.md`; clean on re-run)
- `site-unit` → exit 0, 310 tests, 310 pass, 0 fail
- `doc-conformance` → exit 0, 284 tests, 284 pass, 0 fail
- 2026-09-20

`REF22-CLAYTON` was seen red before the edit on the forbidden arm
(`/nios\.ac\.in/`). The replacement was verified three ways before it was
proposed — the publisher's article page, the served HTML (190 KB, 112
occurrences of *rūpak*), and Crossref — with `https://doi.org/10.30535/mto.26.1.2`
resolving to the article.

Neither Scribd file was usable, and both were checked rather than assumed:
`532106888-Indian-Music-Fundamentals-of-Indian-Music.pdf` is Swatantra Sharma's
student textbook, 168 pages with **zero** extractable characters — an image-only
scan — and `919955238-…-Lewis-Rowell-Open-Library.pdf` is a seven-page Chrome
print of an Open Library catalogue page, not the book.

### Step 5's stated expectation was wrong, and what is true instead

The plan said re-adding `nios.ac.in` alone would turn the queue test red. It does
not. Measured:

| mutation | `REF22-CLAYTON` | `ref-22 stays queued` |
|---|---|---|
| `nios.ac.in` returns | **red** | green |
| `nios.ac.in` returns and `ref-22` is struck from the worklist | **red** | **red** |

The queue test is not dead — it still fires — but it is **dominated**: every
failure it can detect is one `REF22-CLAYTON` already detects, because that case
forbids the host tree-wide. It can never be the only signal. Recorded rather
than resolved; deleting a guard or generalising it is not this task's call.
