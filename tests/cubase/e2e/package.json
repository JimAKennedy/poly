{
  "name": "poly-cubase-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "M042 S09 — Playwright-over-CDP L4-web e2e (attaches to Poly's editor inside Cubase on the Windows runner).",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:unit": "playwright test lib",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@types/node": "20.19.0",
    "typescript": "5.6.3"
  }
}
